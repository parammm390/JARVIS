name: security
on:
  pull_request:
    paths: ["finnor-os/**", ".github/workflows/security.yml"]
  push:
    branches: [main]
    paths: ["finnor-os/**", ".github/workflows/security.yml"]
jobs:
  gitleaks:
    name: gitleaks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  osv:
    name: osv-scanner
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: google/osv-scanner-action@v2.3.8
        with:
          scan-args: |-
            --lockfile=finnor-os/package-lock.json

import { ResetPasswordForm } from "@/components/jarvis/lib/ResetPasswordForm"

export const metadata = {
  title: "Reset password — JARVIS",
  description: "Set a new password for your FINNOR JARVIS account.",
}

export default function ResetPasswordPage() {
  return <ResetPasswordForm />
}

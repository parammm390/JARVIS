npm error Missing script: "release:manifest"
npm error
npm error To see a list of scripts, run:
npm error   npm run
npm error A complete log of this run can be found in: /Users/paramdave/.npm/_logs/2026-08-06T16_50_40_858Z-debug-0.log
finnor-os/packages/tools/src/stripe.ts:    "metadata[idempotencyKey]": input.idempotencyKey,
finnor-os/packages/tools/src/stripe.ts:      "Idempotency-Key": input.idempotencyKey,
finnor-os/packages/tools/src/capabilities/communications.ts:  idempotencyKey: z.string().min(1),
finnor-os/packages/tools/src/capabilities/communications.ts:  idempotencyKeyFrom: (input) => input.idempotencyKey,
finnor-os/packages/tools/src/capabilities/communications.ts:        metadata: { tenantId: input.tenantId, idempotencyKey: input.idempotencyKey },
finnor-os/packages/tools/src/capabilities/communications.ts:      return { callId: String((r.output as Record<string, unknown>).id ?? input.idempotencyKey) };
finnor-os/packages/tools/src/capabilities/inventory.ts:  idempotencyKey: z.string().min(1),
finnor-os/packages/tools/src/capabilities/inventory.ts:  idempotencyKey: z.string().min(1),
finnor-os/packages/tools/src/capabilities/inventory.ts:  idempotencyKeyFrom: (input) => input.idempotencyKey,
finnor-os/packages/tools/src/capabilities/inventory.ts:  idempotencyKeyFrom: (input) => input.idempotencyKey,
finnor-os/packages/tools/src/capabilities/scheduling.ts:  idempotencyKey: z.string().min(1),
finnor-os/packages/tools/src/capabilities/scheduling.ts:  idempotencyKey: z.string().min(1),
finnor-os/packages/tools/src/capabilities/scheduling.ts:  idempotencyKeyFrom: (input) => input.idempotencyKey,
finnor-os/packages/tools/src/capabilities/scheduling.ts:  // operationKey is the step's idempotencyKey, not the appointment id, in the native
finnor-os/packages/tools/src/capabilities/scheduling.ts:  idempotencyKeyFrom: (input) => input.idempotencyKey,
finnor-os/packages/tools/src/capabilities/documents.ts:  idempotencyKey: z.string().min(1),
finnor-os/packages/tools/src/capabilities/documents.ts:  idempotencyKey: z.string().min(1),
finnor-os/packages/tools/src/capabilities/documents.ts:  idempotencyKeyFrom: (input) => input.idempotencyKey,
finnor-os/packages/tools/src/capabilities/documents.ts:  retryOnUnknown: true, // generating the same document twice is safe — idempotency key IS the document id
finnor-os/packages/tools/src/capabilities/documents.ts:        provenance: { sourceSystem: "capability:generate_document", externalId: input.idempotencyKey },
finnor-os/packages/tools/src/capabilities/documents.ts:  idempotencyKeyFrom: (input) => input.idempotencyKey,
finnor-os/packages/tools/src/capabilities/crm.ts:  idempotencyKey: z.string().min(1),
finnor-os/packages/tools/src/capabilities/crm.ts:  idempotencyKey: z.string().min(1),
finnor-os/packages/tools/src/capabilities/crm.ts:  idempotencyKey: z.string().min(1),
finnor-os/packages/tools/src/capabilities/crm.ts:  idempotencyKeyFrom: (input) => input.idempotencyKey,
finnor-os/packages/tools/src/capabilities/crm.ts:  // Upsert-by-phone is naturally idempotent on the provider side — a retry after an
finnor-os/packages/tools/src/capabilities/crm.ts:          return { contactId: String(result.contactId ?? input.idempotencyKey), createdNew: true };
finnor-os/packages/tools/src/capabilities/crm.ts:  idempotencyKeyFrom: (input) => input.idempotencyKey,
finnor-os/packages/tools/src/capabilities/crm.ts:  idempotencyKeyFrom: (input) => input.idempotencyKey,
finnor-os/packages/tools/src/capabilities/crm.ts:            visitId: String(result.id ?? input.idempotencyKey),
finnor-os/packages/tools/src/capabilities/marketing.ts:  idempotencyKey: z.string().min(1),
finnor-os/packages/tools/src/capabilities/marketing.ts:  idempotencyKeyFrom: (input) => input.idempotencyKey,
finnor-os/packages/tools/src/capabilities/marketing.ts:  retryOnUnknown: true, // dry-run is idempotent by construction; a real launch would need its own idempotency key support from the provider before this could be true — tracked when write-scope credentials land
finnor-os/packages/tools/src/capabilities/marketing.ts:    return { campaignId: input.idempotencyKey, mode: result.mode };
finnor-os/packages/tools/src/capabilities/accounting.ts:  idempotencyKey: z.string().min(1),
finnor-os/packages/tools/src/capabilities/accounting.ts:  idempotencyKey: z.string().min(1),
finnor-os/packages/tools/src/capabilities/accounting.ts:  idempotencyKeyFrom: (input) => input.idempotencyKey,
finnor-os/packages/tools/src/capabilities/accounting.ts:  idempotencyKeyFrom: (input) => input.idempotencyKey,
finnor-os/packages/tools/src/capabilities/accounting.ts:  retryOnUnknown: true, // creating the same link twice is safe — idempotency key IS the link id
finnor-os/packages/tools/src/docusign.ts:      "X-DocuSign-Idempotency-Key": input.idempotencyKey,
finnor-os/packages/tools/src/idempotent-call.ts:// External-operation idempotency ledger — one row per (domain_action_id, operation_key)
finnor-os/packages/tools/src/emulators/scheduling-emulator.ts:// idempotent-dedup behavior (duplicate delivery of the same hold request is absorbed,
finnor-os/packages/tools/src/emulators/scheduling-emulator.ts:  idempotencyKey: string;
finnor-os/packages/tools/src/emulators/scheduling-emulator.ts:  // Idempotent by the caller's idempotency key — a duplicate delivery of the same hold
finnor-os/packages/tools/src/emulators/scheduling-emulator.ts:  const existing = holds.get(input.idempotencyKey);
finnor-os/packages/tools/src/emulators/scheduling-emulator.ts:  const hold: EmulatorHold = { holdId: input.idempotencyKey, status: "held", scheduledAt: input.scheduledAt, technicianId: input.technicianId };
finnor-os/packages/tools/src/emulators/scheduling-emulator.ts:  idempotencyKey: string;
finnor-os/packages/tools/src/emulators/inventory-emulator.ts:  idempotencyKey: string;
finnor-os/packages/tools/src/emulators/inventory-emulator.ts:  idempotencyKey: string;
finnor-os/packages/tools/src/emulators/inventory-emulator.ts:  const existing = reservations.get(input.idempotencyKey);
finnor-os/packages/tools/src/emulators/inventory-emulator.ts:  const result: ReserveStockOutput = { reserved: true, reservationId: input.idempotencyKey, remaining };
finnor-os/packages/tools/src/emulators/inventory-emulator.ts:  reservations.set(input.idempotencyKey, result);
finnor-os/packages/tools/src/emulators/inventory-emulator.ts:  const existing = receipts.get(input.idempotencyKey);
finnor-os/packages/tools/src/emulators/inventory-emulator.ts:  const result: ReceiveProcurementOutput = { procurementOrderId: input.idempotencyKey, status: "received", newQuantity };
finnor-os/packages/tools/src/emulators/inventory-emulator.ts:  receipts.set(input.idempotencyKey, result);
finnor-os/packages/tools/src/emulators/crm-emulator.ts:  idempotencyKey: string;
finnor-os/packages/tools/src/emulators/crm-emulator.ts:  idempotencyKey: string;
finnor-os/packages/tools/src/emulators/crm-emulator.ts:  idempotencyKey: string;
finnor-os/packages/tools/src/emulators/crm-emulator.ts:const sentMessages = new Set<string>(); // idempotencyKey
finnor-os/packages/tools/src/emulators/crm-emulator.ts:const bookedVisits = new Map<string, { visitId: string; scheduledAt: string }>(); // idempotencyKey -> visit
finnor-os/packages/tools/src/emulators/crm-emulator.ts:export function wasEmulatorMessageSent(idempotencyKey: string): boolean {
finnor-os/packages/tools/src/emulators/crm-emulator.ts:  return sentMessages.has(idempotencyKey);
finnor-os/packages/tools/src/emulators/crm-emulator.ts:  const contactId = input.idempotencyKey;
finnor-os/packages/tools/src/emulators/crm-emulator.ts:  sentMessages.add(input.idempotencyKey);
finnor-os/packages/tools/src/emulators/crm-emulator.ts:  const existing = bookedVisits.get(input.idempotencyKey);
finnor-os/packages/tools/src/emulators/crm-emulator.ts:  const visit = { visitId: input.idempotencyKey, scheduledAt: input.startTime };
finnor-os/packages/tools/src/emulators/crm-emulator.ts:  bookedVisits.set(input.idempotencyKey, visit);
finnor-os/packages/tools/src/emulators/accounting-emulator.ts:  idempotencyKey: string;
finnor-os/packages/tools/src/emulators/accounting-emulator.ts:  idempotencyKey: string;
finnor-os/packages/tools/src/emulators/accounting-emulator.ts:  const existing = syncedInvoices.get(input.idempotencyKey);
finnor-os/packages/tools/src/emulators/accounting-emulator.ts:  const result: SyncInvoiceOutput = { externalInvoiceId: input.idempotencyKey, externalCustomerId: customerId };
finnor-os/packages/tools/src/emulators/accounting-emulator.ts:  syncedInvoices.set(input.idempotencyKey, result);
finnor-os/packages/tools/src/emulators/accounting-emulator.ts:  const existing = paymentLinks.get(input.idempotencyKey);
finnor-os/packages/tools/src/emulators/accounting-emulator.ts:    linkId: input.idempotencyKey,
finnor-os/packages/tools/src/emulators/accounting-emulator.ts:    paymentLinkUrl: `https://pay.sandbox.finnor.local/${input.idempotencyKey}`,
finnor-os/packages/tools/src/emulators/accounting-emulator.ts:  paymentLinks.set(input.idempotencyKey, result);
finnor-os/packages/tools/src/emulators/documents-emulator.ts:  idempotencyKey: string;
finnor-os/packages/tools/src/emulators/documents-emulator.ts:  idempotencyKey: string;
finnor-os/packages/tools/src/emulators/documents-emulator.ts:  const existing = generatedDocuments.get(input.idempotencyKey);
finnor-os/packages/tools/src/emulators/documents-emulator.ts:  const result: GenerateDocumentOutput = { documentId: input.idempotencyKey, storageRef: `sandbox://documents/${input.idempotencyKey}` };
finnor-os/packages/tools/src/emulators/documents-emulator.ts:  generatedDocuments.set(input.idempotencyKey, result);
finnor-os/packages/tools/src/emulators/documents-emulator.ts:  const existing = signatureRequests.get(input.idempotencyKey);
finnor-os/packages/tools/src/emulators/documents-emulator.ts:  const output: RequestSignatureOutput = { signatureRequestId: input.idempotencyKey, status: "sent" };
finnor-os/packages/tools/src/emulators/documents-emulator.ts:  signatureRequests.set(input.idempotencyKey, { output, status: "sent" });
finnor-os/packages/tools/src/emulators/marketing-emulator.ts:  idempotencyKey: string;
finnor-os/packages/tools/src/emulators/marketing-emulator.ts:  const existing = launchedCampaigns.get(input.idempotencyKey);
finnor-os/packages/tools/src/emulators/marketing-emulator.ts:  const result: LaunchAdCampaignOutput = { campaignId: input.idempotencyKey, mode: "dry_run" };
finnor-os/packages/tools/src/emulators/marketing-emulator.ts:  launchedCampaigns.set(input.idempotencyKey, result);
finnor-os/packages/tools/src/emulators/communications-emulator.ts:// fault-injection profile as the scheduling emulator, plus provider-side idempotency-key
finnor-os/packages/tools/src/emulators/communications-emulator.ts:  idempotencyKey: string;
finnor-os/packages/tools/src/emulators/communications-emulator.ts:export function wasEmulatorCallSent(idempotencyKey: string): boolean {
finnor-os/packages/tools/src/emulators/communications-emulator.ts:  return sentCalls.has(idempotencyKey);
finnor-os/packages/tools/src/emulators/communications-emulator.ts:  // Provider-side idempotency: the same idempotency key never results in a second call.
finnor-os/packages/tools/src/emulators/communications-emulator.ts:  const existing = sentCalls.get(input.idempotencyKey);
finnor-os/packages/tools/src/emulators/communications-emulator.ts:  const call: EmulatorCall = { callId: input.idempotencyKey, phoneNumber: input.phoneNumber, message: input.message };
finnor-os/packages/tools/src/emulators/communications-emulator.ts:  sentCalls.set(input.idempotencyKey, call);
finnor-os/packages/tools/src/observability.ts:// initObservability() is idempotent (safe to call from multiple entry points/chokepoints).
finnor-os/packages/tools/src/registry.ts:import { claimExternalOperation, recordExternalOperationResult, awaitExternalOperationResolution } from "./idempotent-call";
finnor-os/packages/tools/src/registry.ts: * Wraps a real ToolRegistry with an idempotency claim against the external_operations
finnor-os/packages/tools/src/index.ts:export * from "./idempotent-call";
finnor-os/packages/tools/src/binding-resolution.ts: * read in this package (see idempotent-call.ts).
finnor-os/packages/domain-plugins/invoice-to-cash/index.ts: * step list. Idempotent by invoiceId, same as the plugin's own idempotencyKey.
finnor-os/packages/domain-plugins/invoice-to-cash/index.ts:  const idempotencyKey = `invoice-to-cash:${invoiceId}`;
finnor-os/packages/domain-plugins/invoice-to-cash/index.ts:      idempotencyKey,
finnor-os/packages/domain-plugins/invoice-to-cash/index.ts:          payload: { tenantId, invoiceId, amountUsd: Number(invoice.amountUsd), idempotencyKey: `${idempotencyKey}:link` },
finnor-os/packages/domain-plugins/invoice-to-cash/index.ts:            idempotencyKey: `${idempotencyKey}:deliver`,
finnor-os/packages/domain-plugins/invoice-to-cash/index.ts:            idempotencyKey: `${idempotencyKey}:qbo`,
finnor-os/packages/domain-plugins/invoice-to-cash/index.ts:    await enqueueStep(tenantId, submitted.stepIds[0]!, `${idempotencyKey}:link`);
finnor-os/packages/domain-plugins/invoice-to-cash/index.ts: * idempotencyKey is `invoice-to-cash:{invoiceId}` — startInvoiceToCash above),
finnor-os/packages/domain-plugins/invoice-to-cash/index.ts: *     already documents this as safe/idempotent), never a second receipt row.
finnor-os/packages/domain-plugins/proposal-signature/index.ts:    const idempotencyKey = `proposal-signature:${proposalId}`;
finnor-os/packages/domain-plugins/proposal-signature/index.ts:        idempotencyKey,
finnor-os/packages/domain-plugins/proposal-signature/index.ts:              idempotencyKey: `${idempotencyKey}:doc`,
finnor-os/packages/domain-plugins/proposal-signature/index.ts:              idempotencyKey: `${idempotencyKey}:sig`,
finnor-os/packages/domain-plugins/proposal-signature/index.ts:      await enqueueStep(tenantId, submitted.stepIds[0]!, `${idempotencyKey}:doc`);
finnor-os/packages/domain-plugins/lead-to-water-test/index.ts:    const idempotencyKey = `lead-to-water-test:${householdId}:${scheduledAt}`;
finnor-os/packages/domain-plugins/lead-to-water-test/index.ts:        idempotencyKey,
finnor-os/packages/domain-plugins/lead-to-water-test/index.ts:              idempotencyKey: `${idempotencyKey}:hold`,
finnor-os/packages/domain-plugins/lead-to-water-test/index.ts:              idempotencyKey: `${idempotencyKey}:confirm-call`,
finnor-os/packages/domain-plugins/lead-to-water-test/index.ts:            payload: { tenantId, idempotencyKey: `${idempotencyKey}:confirm` },
finnor-os/packages/domain-plugins/lead-to-water-test/index.ts:      await enqueueStep(tenantId, submitted.stepIds[0]!, `${idempotencyKey}:hold`);
finnor-os/packages/domain-plugins/proposal-to-installation/index.ts:    const idempotencyKey = `installation:${quoteId}`;
finnor-os/packages/domain-plugins/proposal-to-installation/index.ts:      steps.push({ stepType: "receive_procurement", payload: { tenantId, sku, quantityOrdered: shortfall, idempotencyKey: `${idempotencyKey}:procure` } });
finnor-os/packages/domain-plugins/proposal-to-installation/index.ts:    steps.push({ stepType: "reserve_stock", payload: { tenantId, sku, quantity, idempotencyKey: `${idempotencyKey}:reserve` } });
finnor-os/packages/domain-plugins/proposal-to-installation/index.ts:      payload: { tenantId, invoiceId: invoice.id, amountUsd: depositAmountUsd, idempotencyKey: `${idempotencyKey}:deposit` },
finnor-os/packages/domain-plugins/proposal-to-installation/index.ts:      payload: { tenantId, householdId, quoteId, workOrderType: "install", depositAmountUsd, idempotencyKey: `${idempotencyKey}:wo` },
finnor-os/packages/domain-plugins/proposal-to-installation/index.ts:        idempotencyKey,
finnor-os/packages/domain-plugins/proposal-to-installation/index.ts:      await enqueueStep(tenantId, submitted.stepIds[0]!, steps[0]!.payload.idempotencyKey as string);
finnor-os/packages/domain-plugins/web-research/watch-service.ts:// so a background worker can supply its existing idempotent implementations.
finnor-os/packages/domain-plugins/web-research/watch-service.ts:  /** Implementations should deduplicate this idempotency key durably. */
finnor-os/packages/domain-plugins/web-research/watch-service.ts:    idempotencyKey: string;
finnor-os/packages/domain-plugins/web-research/watch-service.ts:  idempotencyKey: string;
finnor-os/packages/domain-plugins/web-research/watch-service.ts:  /** Implementations should deduplicate by alert.idempotencyKey. */
finnor-os/packages/domain-plugins/web-research/watch-service.ts:            idempotencyKey: `competitor-evidence:${key}:${result.snapshot.changeHash}`,
finnor-os/packages/domain-plugins/web-research/watch-service.ts:          const idempotencyKey = `competitor-alert:${key}:${result.snapshot.changeHash}`;
finnor-os/packages/domain-plugins/web-research/watch-service.ts:            alertId: `alert:${digest(idempotencyKey).slice(0, 24)}`,
finnor-os/packages/domain-plugins/web-research/watch-service.ts:            idempotencyKey,
finnor-os/packages/domain-plugins/web-research/watch-service.ts:        // the deterministic evidence/alert idempotency keys make a retry safe.
finnor-os/packages/memory/src/evidence-recorder.ts:  idempotencyKey: string;
finnor-os/packages/memory/src/evidence-recorder.ts:    idempotencyKey: input.idempotencyKey,
finnor-os/packages/memory/src/evidence-recorder.ts:    if (typeof input.idempotencyKey !== "string" || !input.idempotencyKey.trim()) {
finnor-os/packages/memory/src/evidence-recorder.ts:      throw new Error("Firecrawl evidence requires an idempotencyKey");
finnor-os/packages/shared-types/src/dealer-zero-fixtures.ts:// matters for idempotency: a shared mutating sequence desyncs the moment any draw is
finnor-os/packages/policy-schema/src/index.ts:  idempotencyKey: z.string().min(1).max(200).optional(),
finnor-os/packages/orchestration/src/instruction-trace.ts: *  row IS the claim, same `onConflictDoNothing` shape as intake-idempotency's own
finnor-os/packages/orchestration/src/graph/nodes.ts:    // Same idempotency scoping as the legacy GatedExecutor — see its comment.
finnor-os/packages/orchestration/src/runtime-bridge.ts:  // Deliberately NO command-level idempotencyKey here (unlike lead-to-water-test's
finnor-os/packages/orchestration/src/runtime-bridge.ts:  // the hard way: an action-scoped idempotency key here silently ate the reflection
finnor-os/packages/orchestration/src/runtime-bridge.ts:  // idempotency key (e.g. invoiceId, not domainActionId) — that layer is unchanged by
finnor-os/packages/orchestration/src/runtime-bridge.ts:    // brand-new/pending with no idempotency key to collide on, so nothing else could
finnor-os/packages/orchestration/src/index.ts:        output: { idempotent: true, status: row.current.status },
finnor-os/packages/orchestration/src/index.ts:    // under review (that transition is a no-op, handled by the idempotent branch
finnor-os/packages/orchestration/src/index.ts:      // correct idempotent target state, not an error.
finnor-os/packages/orchestration/src/index.ts:        return { status: "success", output: { idempotent: true, status: transition.current.status } };
finnor-os/packages/orchestration/src/index.ts:      return { status: "success", output: { idempotent: true, status: transition.current.status } };
finnor-os/packages/db/schema.ts:    idempotencyKey: text("idempotency_key").unique(),
finnor-os/packages/db/schema.ts:    idempotencyKey: text("idempotency_key"),
finnor-os/packages/db/schema.ts:  (t) => [unique("commands_tenant_idempotency_idx").on(t.tenantId, t.idempotencyKey)],
finnor-os/packages/db/schema.ts:    idempotencyKey: text("idempotency_key").notNull(),
finnor-os/packages/db/schema.ts:// Generalizes external_operations (packages/tools/src/idempotent-call.ts) from being
finnor-os/packages/db/schema.ts:// steps), matching the pack's §2.3 shape exactly so replay can reuse the idempotency key.
finnor-os/packages/db/schema.ts:// A4.T6 (migration 0042): opt-in idempotency for POST /api/actions. `response` starts
finnor-os/packages/db/schema.ts:// (tenantId, idempotencyKey) conflicts and is rejected before the orchestrator runs
finnor-os/packages/db/schema.ts:  "intake_idempotency",
finnor-os/packages/db/schema.ts:    idempotencyKey: text("idempotency_key").notNull(),
finnor-os/packages/db/schema.ts:  (t) => [unique("intake_idempotency_tenant_key_idx").on(t.tenantId, t.idempotencyKey), index("intake_idempotency_tenant_idx").on(t.tenantId)],
finnor-os/packages/db/seed.ts:    // households has no natural unique key — guard idempotency explicitly, or every
finnor-os/packages/db/seed.ts:    // migration's one-time backfill cannot populate them. Keep the seed idempotent
finnor-os/packages/db/migrations-bundle.ts:  { name: "0000_init.sql", sql: "-- Finnor OS initial migration. Applied via CI / packages/db/migrate.ts — never by hand against production.\n-- Everything lives in its own schema (finnor_os), fully qualified — this is what lets\n-- Finnor share a Postgres instance (e.g. an existing Supabase project) with a\n-- completely unrelated application's `public` schema with zero collision risk.\nCREATE SCHEMA IF NOT EXISTS finnor_os;\n-- LOCAL/CI ONLY: persist search_path on the dedicated local role. On a SHARED cloud\n-- database (Supabase), the connecting role also serves other applications — changing\n-- its default search_path could redirect THEIR unqualified queries into finnor_os\n-- (e.g. a bare `users` would hit finnor_os.users). In the cloud, the application sets\n-- search_path per-connection instead (packages/db/index.ts pool 'connect' hook).\nDO $sp$\nBEGIN\n  IF current_database() = 'finnor' THEN\n    EXECUTE 'ALTER ROLE CURRENT_USER SET search_path = finnor_os, public';\n  END IF;\nEND $sp$;\nCREATE EXTENSION IF NOT EXISTS pgcrypto;\n-- pgvector is present on Supabase and the CI image. On a dev machine without it,\n-- the embeddings column falls back to jsonb and semantic search runs in-process\n-- (packages/memory/src/semantic.ts detects which mode the database is in).\nDO $ext$\nBEGIN\n  CREATE EXTENSION IF NOT EXISTS vector;\nEXCEPTION WHEN OTHERS THEN\n  RAISE NOTICE 'pgvector unavailable — embeddings will use jsonb fallback';\nEND $ext$;\n\nCREATE TABLE IF NOT EXISTS finnor_os.tenants (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  name text NOT NULL,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.users (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  email text NOT NULL UNIQUE,\n  role text NOT NULL CHECK (role IN ('owner','dispatcher','technician')),\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.households (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  address text NOT NULL,\n  contact_info jsonb NOT NULL DEFAULT '{}',\n  water_profile jsonb NOT NULL DEFAULT '{}',\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.equipment (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  household_id uuid NOT NULL REFERENCES finnor_os.households(id),\n  type text NOT NULL,\n  model text,\n  install_date timestamptz,\n  source text NOT NULL DEFAULT 'finnor' CHECK (source IN ('finnor','competitor'))\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.technicians (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  name text NOT NULL,\n  contact_info jsonb NOT NULL DEFAULT '{}',\n  availability jsonb NOT NULL DEFAULT '{}'\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.service_visits (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  household_id uuid NOT NULL REFERENCES finnor_os.households(id),\n  technician_id uuid REFERENCES finnor_os.technicians(id),\n  type text NOT NULL,\n  scheduled_at timestamptz,\n  completed_at timestamptz,\n  notes text\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.maintenance_agreements (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  household_id uuid NOT NULL REFERENCES finnor_os.households(id),\n  cadence text NOT NULL,\n  terms jsonb NOT NULL DEFAULT '{}',\n  status text NOT NULL DEFAULT 'active' CHECK (status IN ('active','renewal_window','renewal_sent','renewed','lapsed')),\n  renewal_date timestamptz\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.proposals (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  household_id uuid NOT NULL REFERENCES finnor_os.households(id),\n  content jsonb NOT NULL DEFAULT '{}',\n  status text NOT NULL DEFAULT 'draft',\n  sent_at timestamptz\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.communications_log (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  household_id uuid NOT NULL REFERENCES finnor_os.households(id),\n  channel text NOT NULL,\n  direction text NOT NULL CHECK (direction IN ('inbound','outbound')),\n  content text NOT NULL,\n  \"timestamp\" timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.domain_policies (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  action_type text NOT NULL,\n  policy jsonb NOT NULL DEFAULT '{}',\n  requires_confirmation boolean NOT NULL DEFAULT true,\n  confirmation_template text,\n  model_provider text\n);\nCREATE INDEX IF NOT EXISTS domain_policies_tenant_action_idx ON finnor_os.domain_policies(tenant_id, action_type);\n\nCREATE TABLE IF NOT EXISTS finnor_os.domain_actions (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  action_type text NOT NULL,\n  payload jsonb NOT NULL DEFAULT '{}',\n  policy_id uuid REFERENCES finnor_os.domain_policies(id),\n  status text NOT NULL DEFAULT 'draft' CHECK (status IN ('draft','pending','approved','rejected','executing','completed','failed','needs_human_review','blocked_integration_unavailable')),\n  summary text,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\nCREATE INDEX IF NOT EXISTS domain_actions_tenant_status_idx ON finnor_os.domain_actions(tenant_id, status);\n\nCREATE TABLE IF NOT EXISTS finnor_os.action_log (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  domain_action_id uuid NOT NULL REFERENCES finnor_os.domain_actions(id),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  step text NOT NULL,\n  input jsonb NOT NULL DEFAULT '{}',\n  output jsonb NOT NULL DEFAULT '{}',\n  \"timestamp\" timestamptz NOT NULL DEFAULT now()\n);\nCREATE INDEX IF NOT EXISTS action_log_action_idx ON finnor_os.action_log(domain_action_id);\n\n-- embeddings.embedding is vector(1536) where pgvector exists, jsonb otherwise (see above).\nDO $emb$\nBEGIN\n  IF EXISTS (SELECT 1 FROM pg_extension WHERE extname = 'vector') THEN\n    EXECUTE 'CREATE TABLE IF NOT EXISTS finnor_os.embeddings (\n      id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n      tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n      source_doc_id text,\n      chunk text NOT NULL,\n      embedding vector(1536)\n    )';\n  ELSE\n    EXECUTE 'CREATE TABLE IF NOT EXISTS finnor_os.embeddings (\n      id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n      tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n      source_doc_id text,\n      chunk text NOT NULL,\n      embedding jsonb\n    )';\n  END IF;\nEND $emb$;\nCREATE INDEX IF NOT EXISTS embeddings_tenant_idx ON finnor_os.embeddings(tenant_id);\n\nCREATE TABLE IF NOT EXISTS finnor_os.role_permissions (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  role text NOT NULL CHECK (role IN ('owner','dispatcher','technician')),\n  action_type text NOT NULL,\n  can_approve boolean NOT NULL DEFAULT false\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.jobs (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  type text NOT NULL,\n  payload jsonb NOT NULL DEFAULT '{}',\n  status text NOT NULL DEFAULT 'queued' CHECK (status IN ('queued','running','completed','failed','dead_letter')),\n  attempts integer NOT NULL DEFAULT 0,\n  max_attempts integer NOT NULL DEFAULT 3,\n  run_at timestamptz NOT NULL DEFAULT now(),\n  last_error text,\n  idempotency_key text UNIQUE\n);\nCREATE INDEX IF NOT EXISTS jobs_status_run_at_idx ON finnor_os.jobs(status, run_at);\n\nCREATE TABLE IF NOT EXISTS finnor_os.workflow_states (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  workflow text NOT NULL,\n  subject_type text NOT NULL,\n  subject_id uuid NOT NULL,\n  state text NOT NULL,\n  history jsonb NOT NULL DEFAULT '[]',\n  updated_at timestamptz NOT NULL DEFAULT now()\n);\n\n-- ============================================================================\n-- Row-level security (§7, §15 brief; §20 blueprint).\n-- Application code always runs inside withTenant(), which sets the\n-- transaction-local GUC app.tenant_id. Supabase-authenticated requests may\n-- alternatively carry tenant_id in the JWT. Either grants access; absent both,\n-- tenant tables return nothing.\n-- ============================================================================\n\nCREATE OR REPLACE FUNCTION finnor_os.request_tenant_id() RETURNS uuid\nLANGUAGE sql STABLE AS $$\n  SELECT COALESCE(\n    NULLIF(current_setting('app.tenant_id', true), '')::uuid,\n    NULLIF(((current_setting('request.jwt.claims', true))::jsonb ->> 'tenant_id'), '')::uuid\n  )\n$$;\n\n-- Tables carrying tenant_id directly:\nDO $do$\nDECLARE t text;\nBEGIN\n  FOREACH t IN ARRAY ARRAY[\n    'users','households','technicians','domain_policies','domain_actions',\n    'action_log','embeddings','role_permissions','workflow_states'\n  ] LOOP\n    EXECUTE format('ALTER TABLE finnor_os.%I ENABLE ROW LEVEL SECURITY', t);\n    EXECUTE format('ALTER TABLE finnor_os.%I FORCE ROW LEVEL SECURITY', t);\n    EXECUTE format(\n      'DROP POLICY IF EXISTS tenant_isolation ON finnor_os.%I;\n       CREATE POLICY tenant_isolation ON finnor_os.%I\n         USING (tenant_id = finnor_os.request_tenant_id())\n         WITH CHECK (tenant_id = finnor_os.request_tenant_id())', t, t);\n  END LOOP;\nEND $do$;\n\n-- tenants: a session may only see its own tenant row.\nALTER TABLE finnor_os.tenants ENABLE ROW LEVEL SECURITY;\nALTER TABLE finnor_os.tenants FORCE ROW LEVEL SECURITY;\nDROP POLICY IF EXISTS tenant_isolation ON finnor_os.tenants;\nCREATE POLICY tenant_isolation ON finnor_os.tenants\n  USING (id = finnor_os.request_tenant_id())\n  WITH CHECK (id = finnor_os.request_tenant_id());\n\n-- Tables scoped through households:\nDO $do$\nDECLARE t text;\nBEGIN\n  FOREACH t IN ARRAY ARRAY[\n    'equipment','service_visits','maintenance_agreements','proposals','communications_log'\n  ] LOOP\n    EXECUTE format('ALTER TABLE finnor_os.%I ENABLE ROW LEVEL SECURITY', t);\n    EXECUTE format('ALTER TABLE finnor_os.%I FORCE ROW LEVEL SECURITY', t);\n    EXECUTE format(\n      'DROP POLICY IF EXISTS tenant_isolation ON finnor_os.%I;\n       CREATE POLICY tenant_isolation ON finnor_os.%I\n         USING (household_id IN (SELECT id FROM finnor_os.households WHERE tenant_id = finnor_os.request_tenant_id()))\n         WITH CHECK (household_id IN (SELECT id FROM finnor_os.households WHERE tenant_id = finnor_os.request_tenant_id()))', t, t);\n  END LOOP;\nEND $do$;\n\n-- action_log is append-only episodic memory (§10, §19): no UPDATE/DELETE, ever.\nCREATE OR REPLACE FUNCTION finnor_os.forbid_mutation() RETURNS trigger\nLANGUAGE plpgsql AS $$\nBEGIN\n  RAISE EXCEPTION 'action_log is append-only';\nEND $$;\nDROP TRIGGER IF EXISTS action_log_immutable ON finnor_os.action_log;\nCREATE TRIGGER action_log_immutable\n  BEFORE UPDATE OR DELETE ON finnor_os.action_log\n  FOR EACH ROW EXECUTE FUNCTION finnor_os.forbid_mutation();\n" },
finnor-os/packages/db/migrations-bundle.ts:  { name: "0006_security_controls.sql", sql: "-- Production security controls: distributed API limits, replay receipts, and a\n-- durable idempotency ledger for externally-visible tool calls.\n\nCREATE TABLE IF NOT EXISTS finnor_os.api_rate_limits (\n  bucket_key text NOT NULL,\n  window_started_at timestamptz NOT NULL,\n  count integer NOT NULL DEFAULT 0 CHECK (count >= 0),\n  PRIMARY KEY (bucket_key, window_started_at)\n);\nCREATE INDEX IF NOT EXISTS api_rate_limits_expiry_idx ON finnor_os.api_rate_limits (window_started_at);\n\nCREATE TABLE IF NOT EXISTS finnor_os.webhook_receipts (\n  provider text NOT NULL,\n  event_id text NOT NULL,\n  payload_hash text NOT NULL,\n  received_at timestamptz NOT NULL DEFAULT now(),\n  PRIMARY KEY (provider, event_id)\n);\nCREATE INDEX IF NOT EXISTS webhook_receipts_received_at_idx ON finnor_os.webhook_receipts (received_at);\n\nCREATE TABLE IF NOT EXISTS finnor_os.external_operations (\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  domain_action_id uuid NOT NULL REFERENCES finnor_os.domain_actions(id),\n  operation_key text NOT NULL,\n  request_hash text NOT NULL,\n  status text NOT NULL CHECK (status IN ('running','succeeded','failed')),\n  response jsonb,\n  created_at timestamptz NOT NULL DEFAULT now(),\n  updated_at timestamptz NOT NULL DEFAULT now(),\n  PRIMARY KEY (domain_action_id, operation_key)\n);\nCREATE INDEX IF NOT EXISTS external_operations_tenant_idx ON finnor_os.external_operations (tenant_id, updated_at);\n\nALTER TABLE finnor_os.external_operations ENABLE ROW LEVEL SECURITY;\nALTER TABLE finnor_os.external_operations FORCE ROW LEVEL SECURITY;\nDROP POLICY IF EXISTS tenant_isolation ON finnor_os.external_operations;\nCREATE POLICY tenant_isolation ON finnor_os.external_operations\n  USING (tenant_id = finnor_os.request_tenant_id())\n  WITH CHECK (tenant_id = finnor_os.request_tenant_id());\n\nDO $do$\nBEGIN\n  IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'finnor_app') THEN\n    GRANT SELECT, INSERT, UPDATE ON finnor_os.external_operations TO finnor_app;\n    GRANT SELECT, INSERT, UPDATE, DELETE ON finnor_os.api_rate_limits TO finnor_app;\n    GRANT SELECT, INSERT, UPDATE, DELETE ON finnor_os.webhook_receipts TO finnor_app;\n  END IF;\nEND $do$;\n" },
finnor-os/packages/db/migrations-bundle.ts:  { name: "0009_durable_execution_runtime.sql", sql: "-- Durable execution runtime (Phase 2, docs/jarvis-90-execution-blueprint.md §3).\n-- Extends the existing atomic UPDATE...WHERE status=<expected> concurrency pattern\n-- (domain_actions) and the existing Postgres job queue (jobs table) rather than\n-- introducing a second queue system. All new tables use direct tenant_id RLS, matching\n-- migrations/0008's convention.\n\nCREATE TABLE IF NOT EXISTS finnor_os.commands (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  command_type text NOT NULL,\n  payload jsonb NOT NULL DEFAULT '{}',\n  idempotency_key text,\n  requested_by text,\n  status text NOT NULL DEFAULT 'approved' CHECK (status IN ('approved','running','completed','failed')),\n  created_at timestamptz NOT NULL DEFAULT now(),\n  updated_at timestamptz NOT NULL DEFAULT now(),\n  UNIQUE (tenant_id, idempotency_key)\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.workflow_runs (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  command_id uuid NOT NULL REFERENCES finnor_os.commands(id),\n  workflow_type text NOT NULL,\n  status text NOT NULL DEFAULT 'running' CHECK (status IN ('running','completed','failed','compensating','compensated')),\n  created_at timestamptz NOT NULL DEFAULT now(),\n  updated_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.workflow_steps (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  workflow_run_id uuid NOT NULL REFERENCES finnor_os.workflow_runs(id),\n  step_type text NOT NULL,\n  sequence integer NOT NULL,\n  status text NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','leased','completed','failed','compensating','compensated')),\n  idempotency_key text NOT NULL,\n  lease_expires_at timestamptz,\n  attempts integer NOT NULL DEFAULT 0,\n  evidence jsonb NOT NULL DEFAULT '{}',\n  terminal_reason text,\n  payload jsonb NOT NULL DEFAULT '{}',\n  created_at timestamptz NOT NULL DEFAULT now(),\n  updated_at timestamptz NOT NULL DEFAULT now(),\n  UNIQUE (workflow_run_id, sequence)\n);\nCREATE INDEX IF NOT EXISTS workflow_steps_leased_idx ON finnor_os.workflow_steps (lease_expires_at) WHERE status = 'leased';\n\nCREATE TABLE IF NOT EXISTS finnor_os.integration_operations (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  workflow_step_id uuid NOT NULL REFERENCES finnor_os.workflow_steps(id),\n  operation_key text NOT NULL,\n  capability text NOT NULL,\n  request_hash text NOT NULL,\n  status text NOT NULL CHECK (status IN ('running','succeeded','failed','unknown')),\n  response jsonb,\n  created_at timestamptz NOT NULL DEFAULT now(),\n  updated_at timestamptz NOT NULL DEFAULT now(),\n  UNIQUE (workflow_step_id, operation_key)\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.outbox_events (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  workflow_step_id uuid REFERENCES finnor_os.workflow_steps(id),\n  event_type text NOT NULL,\n  payload jsonb NOT NULL DEFAULT '{}',\n  status text NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','delivering','delivered','unknown','failed')),\n  attempts integer NOT NULL DEFAULT 0,\n  created_at timestamptz NOT NULL DEFAULT now(),\n  delivered_at timestamptz\n);\nCREATE INDEX IF NOT EXISTS outbox_events_pending_idx ON finnor_os.outbox_events (tenant_id, status) WHERE status = 'pending';\n\nCREATE TABLE IF NOT EXISTS finnor_os.inbox_events (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  provider text NOT NULL,\n  event_id text NOT NULL,\n  payload_hash text NOT NULL,\n  matched_step_id uuid REFERENCES finnor_os.workflow_steps(id),\n  status text NOT NULL DEFAULT 'received' CHECK (status IN ('received','matched','unmatched','duplicate')),\n  received_at timestamptz NOT NULL DEFAULT now(),\n  UNIQUE (provider, event_id)\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.reconciliation_cases (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  case_type text NOT NULL CHECK (case_type IN ('unknown_delivery','unmatched_inbox_event')),\n  related_outbox_event_id uuid REFERENCES finnor_os.outbox_events(id),\n  related_inbox_event_id uuid REFERENCES finnor_os.inbox_events(id),\n  related_step_id uuid REFERENCES finnor_os.workflow_steps(id),\n  details jsonb NOT NULL DEFAULT '{}',\n  status text NOT NULL DEFAULT 'open' CHECK (status IN ('open','resolved')),\n  created_at timestamptz NOT NULL DEFAULT now(),\n  resolved_at timestamptz\n);\nCREATE INDEX IF NOT EXISTS reconciliation_cases_open_idx ON finnor_os.reconciliation_cases (tenant_id, status) WHERE status = 'open';\n\nCREATE TABLE IF NOT EXISTS finnor_os.compensation_cases (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  workflow_step_id uuid NOT NULL REFERENCES finnor_os.workflow_steps(id),\n  reason text NOT NULL,\n  status text NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','succeeded','failed')),\n  details jsonb NOT NULL DEFAULT '{}',\n  created_at timestamptz NOT NULL DEFAULT now(),\n  resolved_at timestamptz\n);\n\nDO $do$\nDECLARE t text;\nBEGIN\n  FOREACH t IN ARRAY ARRAY[\n    'commands','workflow_runs','workflow_steps','integration_operations','outbox_events',\n    'inbox_events','reconciliation_cases','compensation_cases'\n  ] LOOP\n    EXECUTE format('ALTER TABLE finnor_os.%I ENABLE ROW LEVEL SECURITY', t);\n    EXECUTE format('ALTER TABLE finnor_os.%I FORCE ROW LEVEL SECURITY', t);\n    EXECUTE format(\n      'DROP POLICY IF EXISTS tenant_isolation ON finnor_os.%I;\n       CREATE POLICY tenant_isolation ON finnor_os.%I\n         USING (tenant_id = finnor_os.request_tenant_id())\n         WITH CHECK (tenant_id = finnor_os.request_tenant_id())', t, t);\n    IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'finnor_app') THEN\n      EXECUTE format('GRANT SELECT, INSERT, UPDATE, DELETE ON finnor_os.%I TO finnor_app', t);\n    END IF;\n  END LOOP;\nEND $do$;\n" },
finnor-os/packages/db/migrations-bundle.ts:  { name: "0025_document_contents_and_external_refs.sql", sql: "-- Phase 4 (§4.2/§4.5): two tables.\n--\n-- document_contents: real PDF bytes, Postgres-backed (no blob-storage account exists\n-- or is in scope this phase) -- separate from documents' metadata row, same\n-- convention as decision_receipts living apart from workflow_steps. One row per\n-- document; content is immutable once written (a re-generation with the same\n-- idempotency key is a no-op at the capability layer, never an in-place edit here).\n--\n-- external_refs: the single join between a Finnor-internal entity and a real\n-- provider's object, once Phase 4's provider bindings start flipping from emulator to\n-- real (GHL contacts, QuickBooks customers/invoices, Stripe payment links, DocuSign\n-- envelopes, Vapi call ids). No provider ids scattered across domain tables.\n\nCREATE TABLE IF NOT EXISTS finnor_os.document_contents (\n  document_id uuid PRIMARY KEY REFERENCES finnor_os.documents(id),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  content_type text NOT NULL DEFAULT 'application/pdf',\n  bytes bytea NOT NULL,\n  size_bytes integer NOT NULL,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.external_refs (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  entity text NOT NULL, -- e.g. \"household\", \"invoice\", \"proposal\", \"appointment\"\n  internal_id uuid NOT NULL,\n  provider text NOT NULL, -- e.g. \"ghl\", \"quickbooks\", \"stripe\", \"docusign\", \"vapi\"\n  external_id text NOT NULL,\n  synced_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE UNIQUE INDEX IF NOT EXISTS external_refs_internal_provider_idx\n  ON finnor_os.external_refs (tenant_id, entity, internal_id, provider);\nCREATE INDEX IF NOT EXISTS external_refs_external_id_idx\n  ON finnor_os.external_refs (tenant_id, provider, external_id);\n\nDO $do$\nBEGIN\n  ALTER TABLE finnor_os.document_contents ENABLE ROW LEVEL SECURITY;\n  ALTER TABLE finnor_os.document_contents FORCE ROW LEVEL SECURITY;\n  DROP POLICY IF EXISTS tenant_isolation ON finnor_os.document_contents;\n  CREATE POLICY tenant_isolation ON finnor_os.document_contents\n    USING (tenant_id = finnor_os.request_tenant_id())\n    WITH CHECK (tenant_id = finnor_os.request_tenant_id());\n\n  ALTER TABLE finnor_os.external_refs ENABLE ROW LEVEL SECURITY;\n  ALTER TABLE finnor_os.external_refs FORCE ROW LEVEL SECURITY;\n  DROP POLICY IF EXISTS tenant_isolation ON finnor_os.external_refs;\n  CREATE POLICY tenant_isolation ON finnor_os.external_refs\n    USING (tenant_id = finnor_os.request_tenant_id())\n    WITH CHECK (tenant_id = finnor_os.request_tenant_id());\n\n  IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'finnor_app') THEN\n    GRANT SELECT, INSERT, UPDATE, DELETE ON finnor_os.document_contents TO finnor_app;\n    GRANT SELECT, INSERT, UPDATE, DELETE ON finnor_os.external_refs TO finnor_app;\n  END IF;\nEND $do$;\n" },
finnor-os/packages/db/migrations-bundle.ts:  { name: "0036_finnor_langgraph_grants.sql", sql: "-- A1 critical finding follow-through (2026-07-22): migration 0032's finnor_langgraph\n-- grant block was a no-op in every environment because the schema didn't exist yet at\n-- that point (this is the same gap that caused the \"relation does not exist\" crash,\n-- fixed for real via POST /api/admin/migrate now also running PostgresSaver.setup()).\n-- Re-issues exactly the grants 0032 would have applied had the schema existed then.\n-- Safe/idempotent regardless of whether finnor_langgraph or finnor_app exist yet in a\n-- given environment (both checked, matching 0032's own conditional style).\n\nDO $do$\nBEGIN\n  IF EXISTS (SELECT 1 FROM information_schema.schemata WHERE schema_name = 'finnor_langgraph')\n     AND EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'finnor_app') THEN\n    EXECUTE 'GRANT USAGE ON SCHEMA finnor_langgraph TO finnor_app';\n    EXECUTE 'GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA finnor_langgraph TO finnor_app';\n    EXECUTE 'GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA finnor_langgraph TO finnor_app';\n    EXECUTE 'ALTER DEFAULT PRIVILEGES IN SCHEMA finnor_langgraph GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO finnor_app';\n  END IF;\nEND $do$;\n" },
finnor-os/packages/db/migrations-bundle.ts:  { name: "0042_intake_idempotency.sql", sql: "-- A4.T6 (JARVIS MAESTRO PLAN §4/A4): opt-in idempotency for POST /api/actions (the\n-- instruction intake path). `response` starts NULL at claim time (the row IS the claim\n-- — a second INSERT for the same (tenant_id, idempotency_key) conflicts and is rejected\n-- BEFORE the orchestrator ever runs a second time), then gets filled in once the real\n-- planner run completes. Distinct from commands.idempotency_key (workflow-runtime's own,\n-- unrelated to the LLM planner's intake) and from tenant_integrations' per-tenant style —\n-- this one's job is purely \"did this exact retried submission already happen.\"\nCREATE TABLE IF NOT EXISTS finnor_os.intake_idempotency (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  idempotency_key text NOT NULL,\n  response jsonb,\n  created_at timestamptz NOT NULL DEFAULT now(),\n  completed_at timestamptz,\n  UNIQUE (tenant_id, idempotency_key)\n);\nCREATE INDEX IF NOT EXISTS intake_idempotency_tenant_idx ON finnor_os.intake_idempotency (tenant_id);\n\nDO $do$\nBEGIN\n  ALTER TABLE finnor_os.intake_idempotency ENABLE ROW LEVEL SECURITY;\n  ALTER TABLE finnor_os.intake_idempotency FORCE ROW LEVEL SECURITY;\n  DROP POLICY IF EXISTS tenant_isolation ON finnor_os.intake_idempotency;\n  CREATE POLICY tenant_isolation ON finnor_os.intake_idempotency\n    USING (tenant_id = finnor_os.request_tenant_id())\n    WITH CHECK (tenant_id = finnor_os.request_tenant_id());\n  IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'finnor_app') THEN\n    GRANT SELECT, INSERT, UPDATE, DELETE ON finnor_os.intake_idempotency TO finnor_app;\n  END IF;\nEND $do$;\n" },
finnor-os/packages/db/migrations-bundle.ts:  { name: "0049_api_rate_limits_repair.sql", sql: "-- A5.T2: Preview schema verification found a recorded-but-missing 0006 table.\n-- Reassert this global rate-limit table idempotently so the authenticated tenant\n-- probe exercises the actual route instead of failing before its RLS read.\nCREATE TABLE IF NOT EXISTS finnor_os.api_rate_limits (\n  bucket_key text NOT NULL,\n  window_started_at timestamptz NOT NULL,\n  count integer NOT NULL DEFAULT 0 CHECK (count >= 0),\n  PRIMARY KEY (bucket_key, window_started_at)\n);\nCREATE INDEX IF NOT EXISTS api_rate_limits_expiry_idx ON finnor_os.api_rate_limits (window_started_at);\n\nDO $do$\nBEGIN\n  IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'finnor_app') THEN\n    GRANT SELECT, INSERT, UPDATE, DELETE ON finnor_os.api_rate_limits TO finnor_app;\n  END IF;\nEND $do$;\n" },
finnor-os/packages/db/index.ts:  idempotencyKey?: string,
finnor-os/packages/db/index.ts:    `INSERT INTO jobs (type, payload, idempotency_key, lane, priority) VALUES ($1, $2, $3, $4, $5)
finnor-os/packages/db/index.ts:     ON CONFLICT (idempotency_key) DO NOTHING`,
finnor-os/packages/db/index.ts:    [type, JSON.stringify(fullPayload), idempotencyKey ?? null, lane, priority],
finnor-os/packages/workflow-runtime/src/dlq.ts:  // original idempotency key. Return that original run when there is one so the UI
finnor-os/packages/workflow-runtime/src/dlq.ts: *  the event row's own id is the idempotency key relayOutboxEvents() already passes to
finnor-os/packages/workflow-runtime/src/dlq.ts: *  new one, satisfying the "replay re-enqueues with the same idempotency key" rule
finnor-os/packages/workflow-runtime/src/outbox.ts: *  before landing in the DLQ anyway. `idempotencyKey` lets a real destination (a
finnor-os/packages/workflow-runtime/src/outbox.ts:  deliver(eventType: string, payload: Record<string, unknown>, opts: { idempotencyKey: string }): Promise<void>;
finnor-os/packages/workflow-runtime/src/outbox.ts:      await deliverer.deliver(event.eventType, event.payload as Record<string, unknown>, { idempotencyKey: event.id });
finnor-os/packages/workflow-runtime/src/commands.ts:// gate) — this table exists to give every workflow_run a stable, idempotent parent.
finnor-os/packages/workflow-runtime/src/commands.ts:  idempotencyKey?: string;
finnor-os/packages/workflow-runtime/src/commands.ts:  if (params.idempotencyKey) {
finnor-os/packages/workflow-runtime/src/commands.ts:      .where(and(eq(commands.tenantId, params.tenantId), eq(commands.idempotencyKey, params.idempotencyKey)));
finnor-os/packages/workflow-runtime/src/commands.ts:      idempotencyKey: params.idempotencyKey ?? null,
finnor-os/packages/workflow-runtime/src/commands.ts:        idempotencyKey: `${run!.id}:${i}`,
finnor-os/packages/workflow-runtime/src/capability.ts:// external_operations claim/reclaim dance (packages/tools/src/idempotent-call.ts) from
finnor-os/packages/workflow-runtime/src/capability.ts:  idempotencyKeyFrom(input: TIn): string;
finnor-os/packages/workflow-runtime/src/capability.ts:  const operationKey = contract.idempotencyKeyFrom(input);
finnor-os/packages/workflow-runtime/src/steps.ts:export async function enqueueStep(tenantId: string, stepId: string, idempotencyKey: string): Promise<void> {
finnor-os/packages/workflow-runtime/src/steps.ts:  await enqueueJob("run_workflow_step", { tenantId, workflowStepId: stepId }, `workflow-step:${idempotencyKey}`);
finnor-os/packages/workflow-runtime/src/steps.ts:/** §2.4: finalizes the step's receipt in place — idempotent to call on a resumed step
finnor-os/packages/workflow-runtime/src/steps.ts:    await enqueueStep(tenantId, next.id, next.idempotencyKey);
finnor-os/packages/workflow-runtime/src/steps.ts:      await enqueueStep(tenantId, step.id, step.idempotencyKey);
finnor-os/packages/workflow-runtime/src/steps.ts:    await enqueueStep(tenantId, step.id, step.idempotencyKey);
finnor-os/packages/data-platform/src/documents.ts: *  bytea helper). Idempotent: re-generating the same document (same idempotency key
finnor-os/apps/api/app/api/data-quality/findings/[id]/resolve/route.ts:      return Response.json({ resolved: true, idempotent: true });
finnor-os/apps/api/app/api/admin/migrate/route.ts:    // — PostgresSaver.setup() is idempotent, safe on every future call here.
finnor-os/apps/api/app/api/actions/route.ts:import { claimOrGetCachedIntake, completeIntakeClaim } from "../../../lib/intake-idempotency";
finnor-os/apps/api/app/api/actions/route.ts:    if (!body.data.idempotencyKey) {
finnor-os/apps/api/app/api/actions/route.ts:    const claim = await claimOrGetCachedIntake(ctx.tenantId, body.data.idempotencyKey);
finnor-os/apps/api/app/api/actions/route.ts:        { error: "A submission with this idempotency key is already in progress (or a prior attempt never completed) — don't retry the same key indefinitely." },
finnor-os/apps/api/app/api/actions/[id]/reject/route.ts:    if (row.status === "rejected") return Response.json({ status: "rejected", idempotent: true });
finnor-os/apps/api/app/api/actions/[id]/reject/route.ts:    if (result.output.idempotent) return Response.json({ status: result.output.status, idempotent: true });
finnor-os/apps/api/app/api/actions/[id]/confirm/route.ts:      return Response.json({ status: row.status, idempotent: true }); // safe to call twice
finnor-os/apps/api/app/api/actions/[id]/escalate/route.ts:    if (row.status === "needs_human_review") return Response.json({ status: "needs_human_review", idempotent: true });
finnor-os/apps/api/app/api/webhooks/ghl/route.ts:  // jobs.idempotencyKey dedup below (that only covers events with a webhookId that
finnor-os/apps/api/app/api/webhooks/ghl/route.ts:      idempotencyKey: eventId ? `ghl:${String(eventId)}` : null,
finnor-os/apps/api/app/api/webhooks/ghl/route.ts:    .onConflictDoNothing({ target: jobs.idempotencyKey });
finnor-os/apps/api/app/api/webhooks/vapi/route.ts:  // already uses for its own correlation-free purposes (session/idempotency keys).
finnor-os/apps/api/app/api/webhooks/vapi/route.ts:    // for its custom idempotencyKey shape) has to set it by hand.
finnor-os/apps/api/app/api/webhooks/vapi/route.ts:        idempotencyKey: msg.call?.id ? `vapi:${msg.call.id}` : null,
finnor-os/apps/api/app/api/webhooks/vapi/route.ts:      .onConflictDoNothing({ target: jobs.idempotencyKey });
finnor-os/apps/api/lib/intake-idempotency.ts:// A4.T6: opt-in idempotency for POST /api/actions. The claim IS the INSERT — a second
finnor-os/apps/api/lib/intake-idempotency.ts:// caller with the same (tenantId, idempotencyKey) conflicts on the unique index and is
finnor-os/apps/api/lib/intake-idempotency.ts:/** Attempts to claim (tenantId, idempotencyKey). Three outcomes:
finnor-os/apps/api/lib/intake-idempotency.ts:export async function claimOrGetCachedIntake(tenantId: string, idempotencyKey: string): Promise<IntakeClaimResult> {
finnor-os/apps/api/lib/intake-idempotency.ts:    db.insert(intakeIdempotency).values({ tenantId, idempotencyKey }).onConflictDoNothing().returning({ id: intakeIdempotency.id }),
finnor-os/apps/api/lib/intake-idempotency.ts:    db.select().from(intakeIdempotency).where(and(eq(intakeIdempotency.tenantId, tenantId), eq(intakeIdempotency.idempotencyKey, idempotencyKey))),
finnor-os/apps/api/lib/webhook-replay.ts:// Complements, not replaces, the GHL route's existing jobs.idempotencyKey dedup —
finnor-os/apps/worker/src/scheduler.ts:// unique `idempotency_key` column, via enqueueJob()'s `ON CONFLICT DO NOTHING`.
finnor-os/apps/worker/src/scheduler.ts:// enqueue every scan on every tick, bucketed into a time window via the idempotency
finnor-os/apps/worker/src/scheduler.ts:/** Buckets "now" into a window the size of the interval, so the idempotency key is
finnor-os/apps/worker/src/scheduler.ts:/** A4.T4: same idempotent-bucket mechanism as scheduleTick, but for a GLOBAL job type
finnor-os/apps/worker/src/simulator/plan.ts:// day per tenant, because the worker enqueues it with a day-bucketed idempotency key
finnor-os/apps/worker/src/handlers/learning-digest.ts:  // same "fetch once, check membership" idempotency shape as scheduled-reminder.ts.
finnor-os/apps/worker/src/handlers/relay-outbox-events.ts:    logWithTrace({}).info({ eventType, idempotencyKey: opts.idempotencyKey, payload }, "[relay_outbox_events] would deliver");
finnor-os/apps/worker/src/handlers/scan-approval-expiry.ts:    // Conditional on status still being 'pending' — naturally idempotent across
finnor-os/apps/worker/src/handlers/run-workflow-step.ts:        idempotencyKey: payload.idempotencyKey,
finnor-os/apps/worker/src/handlers/run-workflow-step.ts:        idempotencyKey: payload.idempotencyKey,
finnor-os/apps/worker/src/handlers/run-workflow-step.ts:      return { tenantId: payload.tenantId, contactId: payload.contactId, message, channel: payload.channel, idempotencyKey: payload.idempotencyKey };
finnor-os/apps/worker/src/handlers/run-workflow-step.ts: * idempotency/evidence machinery as a capability step (so it survives a crash and
finnor-os/apps/worker/src/handlers/simulator-tick.ts:// daily (apps/worker/src/index.ts's PROACTIVE_SCANS, day-bucketed idempotency key, same
finnor-os/apps/worker/src/handlers/scan-watchdog.ts: *  at all for its idempotency key — i.e. the enqueueStep() call that should have followed
finnor-os/apps/worker/src/handlers/scan-watchdog.ts:    const { rows } = await getPool().query("SELECT 1 FROM jobs WHERE idempotency_key = $1 LIMIT 1", [`workflow-step:${step.idempotencyKey}`]);
finnor-os/apps/worker/src/handlers/scan-watchdog.ts:    await enqueueStep(tenantId, step.id, step.idempotencyKey);
finnor-os/apps/worker/src/queue.ts:// backoff, dead-letter after max attempts. Every handler idempotent.
finnor-os/apps/worker/src/queue.ts:  async enqueue(type: string, payload: Record<string, unknown>, idempotencyKey?: string, lane: JobLane = "batch", priority = 0): Promise<void> {
finnor-os/apps/worker/src/queue.ts:      `INSERT INTO jobs (type, payload, idempotency_key, lane, priority) VALUES ($1, $2, $3, $4, $5)
finnor-os/apps/worker/src/queue.ts:       ON CONFLICT (idempotency_key) DO NOTHING`,
finnor-os/apps/worker/src/queue.ts:      [type, JSON.stringify(payload), idempotencyKey ?? null, lane, priority],
finnor-os/packages/voice-os/src/index.ts:  decision: "confirmed" | "rejected",
finnor-os/packages/tools/src/wrap.ts:// Uniform wrapper for every tool call: timeout, retry with backoff, typed result (§22, §30).
finnor-os/packages/tools/src/capabilities/scheduling.ts:export const ConfirmAppointmentOutputSchema = z.object({ holdId: z.string(), status: z.literal("confirmed") });
finnor-os/packages/tools/src/capabilities/scheduling.ts:  retryOnUnknown: true, // marking an already-confirmed hold confirmed again is a safe no-op
finnor-os/packages/tools/src/capabilities/scheduling.ts:      const [row] = await db.update(appointments).set({ status: "confirmed" }).where(eq(appointments.id, input.holdId)).returning();
finnor-os/packages/tools/src/capabilities/scheduling.ts:          eventType: "appointment_confirmed",
finnor-os/packages/tools/src/capabilities/scheduling.ts:      return { holdId: input.holdId, status: "confirmed" as const };
finnor-os/packages/tools/src/mcp-client.ts:// Connection failures are caught and typed — never an unhandled rejection.
finnor-os/packages/tools/src/emulators/scheduling-emulator.ts:  status: "held" | "released" | "confirmed";
finnor-os/packages/tools/src/emulators/scheduling-emulator.ts:export function getEmulatorHoldStatus(holdId: string): "held" | "released" | "confirmed" | "not_found" {
finnor-os/packages/tools/src/emulators/scheduling-emulator.ts:  status: "confirmed";
finnor-os/packages/tools/src/emulators/scheduling-emulator.ts:  if (hold) hold.status = "confirmed";
finnor-os/packages/tools/src/emulators/scheduling-emulator.ts:  return { holdId: input.holdId, status: "confirmed" };
finnor-os/packages/tools/src/emulators/inventory-emulator.ts:// domain (confirmed: no Cin7/NetSuite/etc. anywhere in the repo), and unlike
finnor-os/packages/tools/src/emulators/documents-emulator.ts:// (DocuSign/HelloSign/etc.) is integrated anywhere in this repo (confirmed by grep) —
finnor-os/packages/tools/src/vapi-rest.ts:// Wrapped like every other integration: timeout, retry, typed errors — no bare fetch.
finnor-os/packages/tools/src/exa.ts:// Wrapped like every integration: timeout, retry, typed errors.
finnor-os/packages/tools/src/registry.ts: * (not a duck-typed wrapper) because `tools` is a private field — every plugin's
finnor-os/packages/tools/src/builtin-tools.ts:  // plugin's execute(). Real when QuickBooks is connected, an explicit typed
finnor-os/packages/tools/src/ads.ts:// typed errors, throws IntegrationError with `retryable` set correctly.
finnor-os/packages/domain-plugins/bulk-notify/index.ts:// in this system already respects. discountPercent is a real, separately-typed
finnor-os/packages/domain-plugins/bulk-notify/index.ts:// gave (typed or spoken, either way through the same planner), never an LLM
finnor-os/packages/domain-plugins/bulk-notify/index.ts:    // (typed or spoken), never invented downstream. Omit for a non-discount check-in.
finnor-os/packages/domain-plugins/lead-to-water-test/index.ts:            // becomes 'confirmed' only once notification succeeds (never confirmed
finnor-os/packages/domain-plugins/shared/plugin-interface.ts: * Stub plugin factory (§31 pattern): valid, typed, placeholder-marked. Registers its
finnor-os/packages/domain-plugins/web-research/index.ts:function typedFirecrawlResult(output: Record<string, unknown>): FirecrawlScrapeResult | undefined {
finnor-os/packages/domain-plugins/web-research/index.ts:    const verified = response.ok ? typedFirecrawlResult(response.output) : undefined;
finnor-os/packages/domain-plugins/web-research/index.ts:      : { error: response.error ?? "Firecrawl verification returned no typed source" });
finnor-os/packages/domain-plugins/web-research/index.ts:          else failedSources.push({ url, error: "source returned no typed citation" });
finnor-os/packages/memory/src/evidence.ts:        // Keep $2 typed in the no-pgvector fallback: `base` uses $1 (tenant) and
finnor-os/packages/memory/src/patterns.ts:// proposals has no tenant_id column (confirmed: packages/db/schema.ts's proposals
finnor-os/packages/memory/src/patterns.ts:  // "accepted" — confirmed in applySignatureOutcome()), so the real outcome must be
finnor-os/packages/memory/src/semantic.ts: *  without a real provider gets a loud, typed failure. Deliberately does NOT crash the
finnor-os/packages/memory/src/semantic.ts: * parameter should be reconfirmed against Voyage's live docs at signup time (see
finnor-os/packages/memory/src/semantic.ts:  // Raw client, not Drizzle's typed `.select()` — Drizzle's pgvector column mapper
finnor-os/packages/shared-types/src/index.ts:  /** Phase 6 typed plan compiler (§6) output — absent on rows created before this
finnor-os/packages/read-models/src/index.ts:// typed queries over business_events/data_quality_findings/existing domain tables — no
finnor-os/packages/read-models/src/index.ts:        .where(and(eq(appointments.technicianId, t.id), inArray(appointments.status, ["hold", "confirmed"])));
finnor-os/packages/orchestration/src/executor.ts:    // Confirmation-required actions instead carry the `confirmed` episode written
finnor-os/packages/orchestration/src/learning.ts:            inArray(pendingConfirmations.status, ["confirmed", "rejected"]),
finnor-os/packages/orchestration/src/instruction-trace.ts:  source?: "typed" | "voice";
finnor-os/packages/orchestration/src/instruction-trace.ts:          source: opts.source ?? "typed",
finnor-os/packages/orchestration/src/voice.ts:    /\b(yes|yeah|yep|yup|approve|approved|confirm|confirmed|go ahead|do it|send it|book it|sounds good|that works|absolutely|sure|correct|okay|ok)\b/g;
finnor-os/packages/orchestration/src/voice.ts: * Turn a typed integration failure into the exact sentence to speak to the owner —
finnor-os/packages/orchestration/src/runtime-bridge.ts:  // immutable authorization episode. Confirmation-gated actions carry `confirmed`,
finnor-os/packages/orchestration/src/runtime-bridge.ts:        or(eq(actionLog.step, "confirmed"), eq(actionLog.step, "policy_ungated_authorized")),
finnor-os/packages/orchestration/src/index.ts:        source: opts.channel === "voice" ? "voice" : "typed",
finnor-os/packages/orchestration/src/index.ts:   * a human-typed instruction gets, not a hand-inserted row that skips the pipeline.
finnor-os/packages/orchestration/src/index.ts:        .where(and(eq(actionLog.domainActionId, actionId), eq(actionLog.tenantId, tenantId), eq(actionLog.step, "confirmed")))
finnor-os/packages/orchestration/src/index.ts:        step: decision === "approve" ? "confirmed" : decision === "reject" ? "rejected" : "escalated",
finnor-os/packages/orchestration/src/compiler.ts:// (already-typed) column, not a generic lookup map, so this leans on the exact same
finnor-os/packages/orchestration/src/repair.ts:// Every rule below is sourced from a real, confirmed scenario in a fresh
finnor-os/packages/orchestration/src/repair.ts:  /\b(get (it|them|this) (paid|signed|booked|confirmed)|make sure|until|follow (it |them )?through|send (them |him |her )?a payment link)\b/i;
finnor-os/packages/orchestration/src/repair.ts:// silent auto-corrections. None of Step 0's confirmed failures was clean enough to
finnor-os/packages/db/schema.ts:    // Phase 6 typed plan compiler (§6): populated once, right after the Planner's LLM
finnor-os/packages/db/schema.ts:  canApprove: boolean("can_approve").notNull().default(false),
finnor-os/packages/db/schema.ts:    enum: ["hold", "confirmed", "completed", "canceled", "no_show"],
finnor-os/packages/db/schema.ts:  status: text("status", { enum: ["awaiting", "confirmed", "rejected", "expired"] })
finnor-os/packages/db/schema.ts:    source: text("source", { enum: ["typed", "voice"] }).notNull().default("typed"),
finnor-os/packages/db/seed.ts:    // approve nothing. No-rows-for-a-tenant falls back to owner-only (canApprove's
finnor-os/packages/db/migrations-bundle.ts:  { name: "0008_canonical_data_platform.sql", sql: "-- Canonical business data platform (Phase 1, docs/jarvis-90-execution-blueprint.md §1).\n-- households remains the de facto customer/account entity (renaming it is out of scope);\n-- these tables add the canonical layer around it. All new tables use direct tenant_id\n-- RLS (a deliberate deviation from the household-join pattern used by equipment/\n-- service_visits/proposals, for consistency across ~20 new tables and to avoid\n-- join-subquery RLS policies on potentially high-volume tables like business_events).\n\nALTER TABLE finnor_os.tenants ADD COLUMN IF NOT EXISTS timezone text NOT NULL DEFAULT 'America/Chicago';\nALTER TABLE finnor_os.proposals ADD COLUMN IF NOT EXISTS quote_id uuid;\nALTER TABLE finnor_os.embeddings ADD COLUMN IF NOT EXISTS document_id uuid;\n\nCREATE TABLE IF NOT EXISTS finnor_os.contacts (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  household_id uuid REFERENCES finnor_os.households(id),\n  name text NOT NULL,\n  role text,\n  archived_at timestamptz,\n  source_system text,\n  external_id text,\n  created_by text,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.contact_methods (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  contact_id uuid NOT NULL REFERENCES finnor_os.contacts(id),\n  method_type text NOT NULL CHECK (method_type IN ('phone','email','sms')),\n  value text NOT NULL,\n  consent boolean NOT NULL DEFAULT false,\n  consent_recorded_at timestamptz,\n  created_at timestamptz NOT NULL DEFAULT now(),\n  UNIQUE (contact_id, method_type, value)\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.leads (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  household_id uuid REFERENCES finnor_os.households(id),\n  contact_method_id uuid REFERENCES finnor_os.contact_methods(id),\n  name text NOT NULL,\n  phone text,\n  email text,\n  address text,\n  status text NOT NULL DEFAULT 'new' CHECK (status IN ('new','contacted','qualified','disqualified','converted')),\n  disqualify_reason text,\n  source text,\n  notes text,\n  archived_at timestamptz,\n  source_system text,\n  external_id text,\n  created_by text,\n  created_at timestamptz NOT NULL DEFAULT now(),\n  UNIQUE (tenant_id, source_system, external_id)\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.opportunities (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  lead_id uuid REFERENCES finnor_os.leads(id),\n  household_id uuid REFERENCES finnor_os.households(id),\n  pipeline_stage text NOT NULL DEFAULT 'open' CHECK (pipeline_stage IN ('open','quote_sent','won','lost')),\n  expected_value_usd numeric(12,2),\n  won_at timestamptz,\n  lost_at timestamptz,\n  lost_reason text,\n  archived_at timestamptz,\n  source_system text,\n  external_id text,\n  created_by text,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.tasks (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  subject_type text NOT NULL,\n  subject_id uuid NOT NULL,\n  title text NOT NULL,\n  due_at timestamptz,\n  assignee_type text CHECK (assignee_type IN ('user','technician')),\n  assignee_id uuid,\n  status text NOT NULL DEFAULT 'open' CHECK (status IN ('open','done','cancelled')),\n  priority text NOT NULL DEFAULT 'normal' CHECK (priority IN ('low','normal','high')),\n  archived_at timestamptz,\n  source_system text,\n  external_id text,\n  created_by text,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.appointments (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  subject_type text NOT NULL,\n  subject_id uuid NOT NULL,\n  technician_id uuid REFERENCES finnor_os.technicians(id),\n  status text NOT NULL DEFAULT 'hold' CHECK (status IN ('hold','confirmed','completed','canceled','no_show')),\n  scheduled_at timestamptz NOT NULL,\n  duration_minutes integer,\n  hold_expires_at timestamptz,\n  notes text,\n  archived_at timestamptz,\n  source_system text,\n  external_id text,\n  created_by text,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.technician_capacity (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  technician_id uuid NOT NULL REFERENCES finnor_os.technicians(id),\n  day_of_week integer CHECK (day_of_week BETWEEN 0 AND 6),\n  start_time text,\n  end_time text,\n  max_concurrent_jobs integer NOT NULL DEFAULT 1,\n  service_radius_miles integer,\n  archived_at timestamptz\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.price_book_items (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  sku text NOT NULL,\n  label text NOT NULL,\n  price_usd numeric(12,2) NOT NULL CHECK (price_usd >= 0),\n  unit_of_measure text NOT NULL DEFAULT 'each',\n  archived_at timestamptz,\n  source_system text,\n  external_id text,\n  created_by text,\n  created_at timestamptz NOT NULL DEFAULT now(),\n  updated_at timestamptz NOT NULL DEFAULT now(),\n  UNIQUE (tenant_id, sku)\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.quotes (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  household_id uuid REFERENCES finnor_os.households(id),\n  lead_id uuid REFERENCES finnor_os.leads(id),\n  opportunity_id uuid REFERENCES finnor_os.opportunities(id),\n  status text NOT NULL DEFAULT 'draft' CHECK (status IN ('draft','sent','accepted','declined','expired')),\n  total_usd numeric(12,2),\n  valid_until timestamptz,\n  archived_at timestamptz,\n  source_system text,\n  external_id text,\n  created_by text,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.quote_line_items (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  quote_id uuid NOT NULL REFERENCES finnor_os.quotes(id),\n  sku text,\n  label text NOT NULL,\n  quantity integer NOT NULL DEFAULT 1 CHECK (quantity > 0),\n  unit_price_usd numeric(12,2) NOT NULL CHECK (unit_price_usd >= 0),\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\n-- Add the FK now that quotes exists (proposals/quote_id column was added above).\nALTER TABLE finnor_os.proposals\n  ADD CONSTRAINT proposals_quote_id_fkey FOREIGN KEY (quote_id) REFERENCES finnor_os.quotes(id);\n\nCREATE TABLE IF NOT EXISTS finnor_os.work_orders (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  household_id uuid NOT NULL REFERENCES finnor_os.households(id),\n  quote_id uuid REFERENCES finnor_os.quotes(id),\n  type text NOT NULL CHECK (type IN ('install','repair','warranty','other')),\n  status text NOT NULL DEFAULT 'draft' CHECK (status IN ('draft','scheduled','in_progress','completed','canceled')),\n  technician_id uuid REFERENCES finnor_os.technicians(id),\n  deposit_amount_usd numeric(12,2),\n  stock_reservation jsonb NOT NULL DEFAULT '{}',\n  scheduled_at timestamptz,\n  completed_at timestamptz,\n  archived_at timestamptz,\n  source_system text,\n  external_id text,\n  created_by text,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.payments (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  invoice_id uuid NOT NULL REFERENCES finnor_os.invoices(id),\n  amount_usd numeric(12,2) NOT NULL CHECK (amount_usd > 0),\n  method text NOT NULL DEFAULT 'other' CHECK (method IN ('card','ach','check','cash','other')),\n  status text NOT NULL DEFAULT 'succeeded' CHECK (status IN ('pending','succeeded','failed','refunded')),\n  received_at timestamptz NOT NULL DEFAULT now(),\n  source_system text,\n  external_id text,\n  created_by text,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.warehouses (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  name text NOT NULL,\n  address text,\n  is_default boolean NOT NULL DEFAULT false,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.warehouse_stock (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  warehouse_id uuid NOT NULL REFERENCES finnor_os.warehouses(id),\n  sku text NOT NULL,\n  quantity integer NOT NULL DEFAULT 0 CHECK (quantity >= 0),\n  unit_of_measure text NOT NULL DEFAULT 'each',\n  reorder_threshold integer NOT NULL DEFAULT 0,\n  created_at timestamptz NOT NULL DEFAULT now(),\n  UNIQUE (warehouse_id, sku)\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.procurement_orders (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  warehouse_id uuid NOT NULL REFERENCES finnor_os.warehouses(id),\n  sku text NOT NULL,\n  quantity_ordered integer NOT NULL CHECK (quantity_ordered > 0),\n  status text NOT NULL DEFAULT 'draft' CHECK (status IN ('draft','ordered','received','canceled')),\n  expected_at timestamptz,\n  received_at timestamptz,\n  source_system text,\n  external_id text,\n  created_by text,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.conversations (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  household_id uuid REFERENCES finnor_os.households(id),\n  contact_id uuid REFERENCES finnor_os.contacts(id),\n  channel text NOT NULL CHECK (channel IN ('voice','sms','email','webchat')),\n  status text NOT NULL DEFAULT 'open' CHECK (status IN ('open','closed')),\n  last_activity_at timestamptz NOT NULL DEFAULT now(),\n  source_system text,\n  external_id text,\n  created_by text,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.calls (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  conversation_id uuid REFERENCES finnor_os.conversations(id),\n  direction text NOT NULL CHECK (direction IN ('inbound','outbound')),\n  from_number text,\n  to_number text,\n  transcript text,\n  recording_url text,\n  started_at timestamptz,\n  ended_at timestamptz,\n  ended_reason text,\n  raw jsonb NOT NULL DEFAULT '{}',\n  source_system text,\n  external_id text,\n  created_by text,\n  created_at timestamptz NOT NULL DEFAULT now(),\n  UNIQUE (tenant_id, source_system, external_id)\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.messages (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  conversation_id uuid REFERENCES finnor_os.conversations(id),\n  direction text NOT NULL CHECK (direction IN ('inbound','outbound')),\n  channel text NOT NULL,\n  content text NOT NULL,\n  sent_at timestamptz NOT NULL DEFAULT now(),\n  source_system text,\n  external_id text,\n  created_by text,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.documents (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  household_id uuid REFERENCES finnor_os.households(id),\n  kind text NOT NULL,\n  title text NOT NULL,\n  storage_ref text,\n  archived_at timestamptz,\n  source_system text,\n  external_id text,\n  created_by text,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nALTER TABLE finnor_os.embeddings\n  ADD CONSTRAINT embeddings_document_id_fkey FOREIGN KEY (document_id) REFERENCES finnor_os.documents(id);\n\nCREATE TABLE IF NOT EXISTS finnor_os.business_events (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  entity_type text NOT NULL,\n  entity_id uuid NOT NULL,\n  event_type text NOT NULL,\n  payload jsonb NOT NULL DEFAULT '{}',\n  occurred_at timestamptz NOT NULL DEFAULT now(),\n  source text\n);\nCREATE INDEX IF NOT EXISTS business_events_entity_idx ON finnor_os.business_events (tenant_id, entity_type, entity_id);\nCREATE INDEX IF NOT EXISTS business_events_type_time_idx ON finnor_os.business_events (tenant_id, event_type, occurred_at);\n\nCREATE TABLE IF NOT EXISTS finnor_os.data_quality_findings (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  finding_type text NOT NULL CHECK (finding_type IN ('duplicate_candidate','missing_critical_field','stale_data','ambiguous_match')),\n  entity_type text NOT NULL,\n  entity_id uuid NOT NULL,\n  related_entity_id uuid,\n  details jsonb NOT NULL DEFAULT '{}',\n  severity text NOT NULL DEFAULT 'medium' CHECK (severity IN ('low','medium','high')),\n  created_at timestamptz NOT NULL DEFAULT now(),\n  resolved_at timestamptz\n);\nCREATE INDEX IF NOT EXISTS data_quality_findings_unresolved_idx ON finnor_os.data_quality_findings (tenant_id, resolved_at);\n\n-- RLS: direct tenant_id policy for every new table (deliberate deviation from the\n-- household-join pattern for new tables only — see file header comment).\nDO $do$\nDECLARE t text;\nBEGIN\n  FOREACH t IN ARRAY ARRAY[\n    'contacts','contact_methods','leads','opportunities','tasks','appointments',\n    'technician_capacity','price_book_items','quotes','quote_line_items','work_orders',\n    'payments','warehouses','warehouse_stock','procurement_orders','conversations',\n    'calls','messages','documents','business_events','data_quality_findings'\n  ] LOOP\n    EXECUTE format('ALTER TABLE finnor_os.%I ENABLE ROW LEVEL SECURITY', t);\n    EXECUTE format('ALTER TABLE finnor_os.%I FORCE ROW LEVEL SECURITY', t);\n    EXECUTE format(\n      'DROP POLICY IF EXISTS tenant_isolation ON finnor_os.%I;\n       CREATE POLICY tenant_isolation ON finnor_os.%I\n         USING (tenant_id = finnor_os.request_tenant_id())\n         WITH CHECK (tenant_id = finnor_os.request_tenant_id())', t, t);\n    IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'finnor_app') THEN\n      EXECUTE format('GRANT SELECT, INSERT, UPDATE, DELETE ON finnor_os.%I TO finnor_app', t);\n    END IF;\n  END LOOP;\nEND $do$;\n" },
finnor-os/packages/db/migrations-bundle.ts:  { name: "0010_voice_os.sql", sql: "-- Voice OS (Phase 5, docs/jarvis-90-execution-blueprint.md §5): real caller identity,\n-- session/turn history, and confirmations bound to a specific action instead of \"the\n-- newest pending action tenant-wide.\" Replaces webhooks/vapi/route.ts's hardcoded\n-- owner userId/role and its \"confirm the newest pending domain_actions\" heuristic.\n-- Same RLS convention as migrations/0008-0009.\n\nCREATE TABLE IF NOT EXISTS finnor_os.voice_identities (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  phone_number text NOT NULL,\n  matched_household_id uuid REFERENCES finnor_os.households(id),\n  matched_user_id uuid REFERENCES finnor_os.users(id),\n  role text NOT NULL DEFAULT 'unknown' CHECK (role IN ('owner','dispatcher','technician','customer','unknown')),\n  first_seen_at timestamptz NOT NULL DEFAULT now(),\n  last_seen_at timestamptz NOT NULL DEFAULT now(),\n  UNIQUE (tenant_id, phone_number)\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.voice_sessions (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  call_external_id text NOT NULL,\n  voice_identity_id uuid REFERENCES finnor_os.voice_identities(id),\n  channel text NOT NULL DEFAULT 'vapi',\n  started_at timestamptz NOT NULL DEFAULT now(),\n  ended_at timestamptz,\n  status text NOT NULL DEFAULT 'active' CHECK (status IN ('active','ended')),\n  UNIQUE (call_external_id)\n);\n\nCREATE TABLE IF NOT EXISTS finnor_os.voice_turns (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  voice_session_id uuid NOT NULL REFERENCES finnor_os.voice_sessions(id),\n  sequence integer NOT NULL,\n  role text NOT NULL CHECK (role IN ('caller','assistant')),\n  transcript_text text NOT NULL,\n  resolved_action_ids jsonb NOT NULL DEFAULT '[]',\n  created_at timestamptz NOT NULL DEFAULT now(),\n  UNIQUE (voice_session_id, sequence)\n);\n\n-- The row finnor_confirm resolves against — binds a spoken yes/no to the exact\n-- domain_action this session's own finnor_instruct drafted, not \"whatever is newest.\"\nCREATE TABLE IF NOT EXISTS finnor_os.pending_confirmations (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  voice_session_id uuid NOT NULL REFERENCES finnor_os.voice_sessions(id),\n  domain_action_id uuid NOT NULL REFERENCES finnor_os.domain_actions(id),\n  prompt_text text NOT NULL,\n  status text NOT NULL DEFAULT 'awaiting' CHECK (status IN ('awaiting','confirmed','rejected','expired')),\n  created_at timestamptz NOT NULL DEFAULT now(),\n  resolved_at timestamptz\n);\nCREATE INDEX IF NOT EXISTS pending_confirmations_awaiting_idx ON finnor_os.pending_confirmations (voice_session_id, status) WHERE status = 'awaiting';\n\nCREATE TABLE IF NOT EXISTS finnor_os.handoffs (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  voice_session_id uuid NOT NULL REFERENCES finnor_os.voice_sessions(id),\n  reason text NOT NULL,\n  to_role text,\n  to_user_id uuid REFERENCES finnor_os.users(id),\n  status text NOT NULL DEFAULT 'open' CHECK (status IN ('open','acknowledged','resolved')),\n  created_at timestamptz NOT NULL DEFAULT now(),\n  resolved_at timestamptz\n);\n\nDO $do$\nDECLARE t text;\nBEGIN\n  FOREACH t IN ARRAY ARRAY[\n    'voice_identities','voice_sessions','voice_turns','pending_confirmations','handoffs'\n  ] LOOP\n    EXECUTE format('ALTER TABLE finnor_os.%I ENABLE ROW LEVEL SECURITY', t);\n    EXECUTE format('ALTER TABLE finnor_os.%I FORCE ROW LEVEL SECURITY', t);\n    EXECUTE format(\n      'DROP POLICY IF EXISTS tenant_isolation ON finnor_os.%I;\n       CREATE POLICY tenant_isolation ON finnor_os.%I\n         USING (tenant_id = finnor_os.request_tenant_id())\n         WITH CHECK (tenant_id = finnor_os.request_tenant_id())', t, t);\n    IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'finnor_app') THEN\n      EXECUTE format('GRANT SELECT, INSERT, UPDATE, DELETE ON finnor_os.%I TO finnor_app', t);\n    END IF;\n  END LOOP;\nEND $do$;\n" },
finnor-os/packages/db/migrations-bundle.ts:  { name: "0027_embeddings_voyage_dimensions.sql", sql: "-- Phase 5 (§5.1, §5.2 — JARVIS 95% MAESTRO PACK): Voyage AI voyage-3.5 embeds at 1024\n-- dimensions, not the 1536 placeholder this table was created with before a real\n-- provider was chosen (packages/memory/src/semantic.ts's DeterministicLocalEmbedder\n-- stood in for an undecided provider). Safe to truncate + retype: writeSemantic() had\n-- zero real call sites before this phase (exhaustive grep — only test files ever called\n-- it), so there is no real embedded data to lose here.\n--\n-- Also lands the chunking-spec metadata (§5.2): entity_refs + occurred_at, and makes\n-- source_doc_id NOT NULL — \"no orphan chunks\" is now enforced at the DB layer, not just\n-- by convention. Mirrors migration 0000's pgvector-present-or-jsonb-fallback branch\n-- (dev machines without the extension keep embedding as jsonb — dimension isn't a typed\n-- constraint there either way, so only the pgvector branch needs a real retype).\nTRUNCATE TABLE finnor_os.embeddings;\nDO $do$\nBEGIN\n  IF EXISTS (SELECT 1 FROM pg_extension WHERE extname = 'vector') THEN\n    EXECUTE 'ALTER TABLE finnor_os.embeddings ALTER COLUMN embedding TYPE vector(1024)';\n  END IF;\nEND $do$;\nALTER TABLE finnor_os.embeddings ALTER COLUMN source_doc_id SET NOT NULL;\nALTER TABLE finnor_os.embeddings ADD COLUMN IF NOT EXISTS entity_refs jsonb NOT NULL DEFAULT '[]';\nALTER TABLE finnor_os.embeddings ADD COLUMN IF NOT EXISTS occurred_at timestamptz NOT NULL DEFAULT now();\nCREATE INDEX IF NOT EXISTS embeddings_tenant_occurred_idx ON finnor_os.embeddings (tenant_id, occurred_at);\n" },
finnor-os/packages/db/migrations-bundle.ts:  { name: "0062_instruction_lifecycle.sql", sql: "-- jarvis-v3 P3.T1: the instruction lifecycle trace (plan v3 §7.1/§8 PHASE 3).\n-- `instruction_sessions.id` is the CLIENT-minted `instructionId` (kernel/instruction.ts\n-- mints it, sends it in POST /api/actions, and polls GET /api/instructions/:id/events\n-- with it) — not gen_random_uuid(), so no DEFAULT here; the row is created the moment\n-- handleInstruction first sees an instructionId, before the first event is emitted.\n-- `instruction_events` is append-only (never updated or deleted, same convention as\n-- action_log) with a strictly-increasing `seq` per instruction_id, enforced by the\n-- UNIQUE constraint below — emitInstructionEvent()'s INSERT ... SELECT MAX(seq)+1\n-- pattern relies on it to make a duplicate seq a constraint violation, not a race.\n--\n-- NOTE ON PHASE COUNT: the session's own binding instruction describes this vocabulary\n-- as \"exactly these 14 values\", but the literal enumerated list it gives has 15 distinct\n-- tokens (received, context_retrieved, planning, plan_ready, clarification_required,\n-- action_created, action_gated, dispatched, executing, step_progress, verifying,\n-- verified, completed, failed, cancelled). Per this plan's own rule that the literal\n-- list governs (\"do not invent, rename, or DROP any\"), all 15 are included verbatim\n-- below; the \"14\" count appears to be a miscount, not a shorter list to drop one from.\nCREATE TABLE IF NOT EXISTS finnor_os.instruction_sessions (\n  id uuid PRIMARY KEY,\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  session_id text,\n  user_id uuid REFERENCES finnor_os.users(id),\n  instruction_text text NOT NULL,\n  source text NOT NULL DEFAULT 'typed' CHECK (source IN ('typed', 'voice')),\n  created_at timestamptz NOT NULL DEFAULT now(),\n  updated_at timestamptz NOT NULL DEFAULT now()\n);\nCREATE INDEX IF NOT EXISTS instruction_sessions_tenant_idx ON finnor_os.instruction_sessions(tenant_id);\n\nCREATE TABLE IF NOT EXISTS finnor_os.instruction_events (\n  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),\n  tenant_id uuid NOT NULL REFERENCES finnor_os.tenants(id),\n  instruction_id uuid NOT NULL REFERENCES finnor_os.instruction_sessions(id),\n  seq integer NOT NULL,\n  phase text NOT NULL CHECK (phase IN (\n    'received', 'context_retrieved', 'planning', 'plan_ready', 'clarification_required',\n    'action_created', 'action_gated', 'dispatched', 'executing', 'step_progress',\n    'verifying', 'verified', 'completed', 'failed', 'cancelled'\n  )),\n  payload jsonb NOT NULL DEFAULT '{}',\n  created_at timestamptz NOT NULL DEFAULT now(),\n  UNIQUE (instruction_id, seq)\n);\nCREATE INDEX IF NOT EXISTS instruction_events_tenant_idx ON finnor_os.instruction_events(tenant_id);\n\nALTER TABLE finnor_os.domain_actions ADD COLUMN IF NOT EXISTS instruction_id uuid REFERENCES finnor_os.instruction_sessions(id);\nCREATE INDEX IF NOT EXISTS domain_actions_instruction_idx ON finnor_os.domain_actions(instruction_id) WHERE instruction_id IS NOT NULL;\n\nDO $do$\nBEGIN\n  ALTER TABLE finnor_os.instruction_sessions ENABLE ROW LEVEL SECURITY;\n  ALTER TABLE finnor_os.instruction_sessions FORCE ROW LEVEL SECURITY;\n  DROP POLICY IF EXISTS tenant_isolation ON finnor_os.instruction_sessions;\n  CREATE POLICY tenant_isolation ON finnor_os.instruction_sessions\n    USING (tenant_id = finnor_os.request_tenant_id())\n    WITH CHECK (tenant_id = finnor_os.request_tenant_id());\n\n  ALTER TABLE finnor_os.instruction_events ENABLE ROW LEVEL SECURITY;\n  ALTER TABLE finnor_os.instruction_events FORCE ROW LEVEL SECURITY;\n  DROP POLICY IF EXISTS tenant_isolation ON finnor_os.instruction_events;\n  CREATE POLICY tenant_isolation ON finnor_os.instruction_events\n    USING (tenant_id = finnor_os.request_tenant_id())\n    WITH CHECK (tenant_id = finnor_os.request_tenant_id());\n\n  IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'finnor_app') THEN\n    GRANT SELECT, INSERT, UPDATE, DELETE ON finnor_os.instruction_sessions TO finnor_app;\n    GRANT SELECT, INSERT ON finnor_os.instruction_events TO finnor_app;\n  END IF;\nEND $do$;\n" },
finnor-os/packages/db/seed-scale.ts:  "Asked to reschedule the appointment, new time confirmed.",
finnor-os/packages/db/index.ts:    // queue, so the backlog only grows, never drains, confirmed directly against
finnor-os/packages/db/backup.ts:// Postgres client tools on PATH (confirmed absent even in this dev sandbox — no pg_dump,
finnor-os/packages/db/backup.ts:// identity columns anywhere (grepped, confirmed) — so restore needs no sequence resets.
finnor-os/packages/data-platform/src/leads.ts:// Canonical lead/opportunity records. Dual-write compromise (confirmed, see the plan):
finnor-os/packages/data-platform/src/appointments.ts:    status: "hold" | "confirmed" | "completed" | "canceled" | "no_show";
finnor-os/apps/api/app/api/read-models/[view]/route.ts:// dashboard) can query the same real, typed views workflow 6's daily digest already
finnor-os/apps/api/app/api/data-quality/findings/[id]/resolve/route.ts:import { requireContext, canApprove, errorResponse } from "../../../../../../lib/auth";
finnor-os/apps/api/app/api/data-quality/findings/[id]/resolve/route.ts:    if (!(await canApprove(ctx, "*"))) {
finnor-os/apps/api/app/api/dlq/route.ts:// judgment call this codebase reserves for `canApprove(ctx, "*")`, same gate as any
finnor-os/apps/api/app/api/dlq/route.ts:import { requireContext, canApprove, errorResponse } from "../../../lib/auth";
finnor-os/apps/api/app/api/dlq/route.ts:    if (!(await canApprove(ctx, "*"))) {
finnor-os/apps/api/app/api/dlq/[id]/discard/route.ts:import { requireContext, canApprove, errorResponse } from "../../../../../lib/auth";
finnor-os/apps/api/app/api/dlq/[id]/discard/route.ts:    if (!(await canApprove(ctx, "*"))) {
finnor-os/apps/api/app/api/dlq/[id]/route.ts:import { requireContext, canApprove, errorResponse } from "../../../../lib/auth";
finnor-os/apps/api/app/api/dlq/[id]/route.ts:    if (!(await canApprove(ctx, "*"))) {
finnor-os/apps/api/app/api/dlq/[id]/replay/route.ts:import { requireContext, canApprove, errorResponse } from "../../../../../lib/auth";
finnor-os/apps/api/app/api/dlq/[id]/replay/route.ts:    if (!(await canApprove(ctx, "*"))) {
finnor-os/apps/api/app/api/me/route.ts:// route re-checks canApprove independently) — this route only lets the frontend know
finnor-os/apps/api/app/api/actions/[id]/revert/route.ts:import { requireContext, canApprove, errorResponse } from "../../../../../lib/auth";
finnor-os/apps/api/app/api/actions/[id]/revert/route.ts:    if (!(await canApprove(ctx, row.actionType))) {
finnor-os/apps/api/app/api/actions/[id]/reject/route.ts:import { requireContext, canApprove, errorResponse } from "../../../../../lib/auth";
finnor-os/apps/api/app/api/actions/[id]/reject/route.ts:    if (!(await canApprove(ctx, row.actionType))) {
finnor-os/apps/api/app/api/actions/[id]/confirm/route.ts:import { requireContext, canApprove, errorResponse } from "../../../../../lib/auth";
finnor-os/apps/api/app/api/actions/[id]/confirm/route.ts:    if (!(await canApprove(ctx, row.actionType))) {
finnor-os/apps/api/app/api/actions/[id]/escalate/route.ts:import { requireContext, canApprove, errorResponse } from "../../../../../lib/auth";
finnor-os/apps/api/app/api/actions/[id]/escalate/route.ts:    if (!(await canApprove(ctx, row.actionType))) {
finnor-os/apps/api/app/api/webhooks/vapi/route.ts:          decision === "approve" ? "confirmed" : "rejected",
finnor-os/apps/api/app/api/corrections/route.ts:// free-floating claim. Falls back to owner-only (canApprove's own default when no
finnor-os/apps/api/app/api/corrections/route.ts:import { requireContext, canApprove, errorResponse } from "../../../lib/auth";
finnor-os/apps/api/app/api/corrections/route.ts:    if (!(await canApprove(ctx, CORRECTION_ACTION_TYPE))) {
finnor-os/apps/api/app/api/corrections/route.ts:    if (!(await canApprove(ctx, CORRECTION_ACTION_TYPE))) {
finnor-os/apps/api/lib/run-control-route.ts:import { requireContext, canApprove, errorResponse } from "./auth";
finnor-os/apps/api/lib/run-control-route.ts:      // process — same canApprove(ctx, "*") gate as the DLQ routes, not a per-action
finnor-os/apps/api/lib/run-control-route.ts:      if (!(await canApprove(ctx, "*"))) {
finnor-os/apps/api/lib/auth.ts:export async function canApprove(ctx: TenantContext, actionType: string): Promise<boolean> {
finnor-os/apps/worker/src/sse-server.ts:  // respond" in staging even though the process itself was confirmed alive via a real
finnor-os/apps/worker/src/handlers/scan-data-quality.ts:      .where(and(eq(appointments.tenantId, tenantId), or(eq(appointments.status, "hold"), eq(appointments.status, "confirmed"))));
finnor-os/apps/worker/src/handlers/scheduled-reminder.ts:        summary: `${ctx.householdLabel}'s maintenance renewal has not been confirmed — sending a firmer follow-up.`,
finnor-os/apps/worker/src/handlers/send-push-notification.ts:// optional action id; this handler resolves recipients and removes only confirmed-dead
finnor-os/apps/worker/src/handlers/scan-appointment-no-shows.ts:// §4.1: "...confirmation, reminder and no-show"). A confirmed appointment whose
finnor-os/apps/worker/src/handlers/scan-appointment-no-shows.ts:      .where(and(eq(appointments.tenantId, tenantId), eq(appointments.status, "confirmed"), lt(appointments.scheduledAt, cutoff))),
finnor-os/apps/worker/src/handlers/send-message.ts:// send_message job: outbound SMS through the tool registry (wrapped, retried, typed).
finnor-os/apps/worker/src/handlers/reconciliation.ts:// the async half of the Reflection promise ("confirmed the action actually landed").
finnor-os/packages/orchestration/src/learning.ts:  /** rejected / decided — of everything a human actually looked at, what fraction they rejected. */
finnor-os/packages/orchestration/src/plugin-registry.ts:        ? `${actionType.replaceAll("_", " ")} is schema-valid; this default prediction makes no claim about external effects.`
finnor-os/packages/orchestration/src/instruction-trace.ts:// actual enumerated tokens. Do not invent, rename, or drop any (binding rule).
finnor-os/packages/orchestration/src/instruction-trace.ts: *  grounded query result remains in the executor/receipt path; it is never copied
finnor-os/packages/orchestration/src/instruction-trace.ts:// second receipt or memory transport.
finnor-os/packages/orchestration/src/dealer-zero-replay.ts:// B4.T3: normalized receipt replay. IDs, timestamps and trace IDs are intentionally
finnor-os/packages/orchestration/src/dealer-zero-replay.ts:// result, actual result, failure, or approval contract is a behavioral regression.
finnor-os/packages/orchestration/src/dealer-zero-replay.ts:  actualResult: unknown;
finnor-os/packages/orchestration/src/dealer-zero-replay.ts:export interface NormalizedReceipt { action: unknown; expected: unknown; actual: unknown; failure: unknown; approval: unknown; }
finnor-os/packages/orchestration/src/dealer-zero-replay.ts:export function normalizeReceipt(receipt: ReceiptLike): NormalizedReceipt {
finnor-os/packages/orchestration/src/dealer-zero-replay.ts:  return { action: canonical(receipt.proposedAction), expected: canonical(receipt.expectedResult), actual: canonical(receipt.actualResult), failure: canonical(receipt.failure), approval: canonical(receipt.approval) };
finnor-os/packages/orchestration/src/dealer-zero-replay.ts:function key(receipt: NormalizedReceipt): string { return JSON.stringify(receipt); }
finnor-os/packages/orchestration/src/dealer-zero-replay.ts:    const receipts = await db.select({ proposedAction: decisionReceipts.proposedAction, expectedResult: decisionReceipts.expectedResult, actualResult: decisionReceipts.actualResult, failure: decisionReceipts.failure, approval: decisionReceipts.approval })
finnor-os/packages/orchestration/src/dealer-zero-replay.ts:    const [recording] = await db.insert(dealerZeroReplayRecordings).values({ tenantId, dateSeed, scenario, eventStream, receiptSnapshot: receipts.map(normalizeReceipt) })
finnor-os/packages/orchestration/src/dealer-zero-replay.ts:      .onConflictDoUpdate({ target: [dealerZeroReplayRecordings.tenantId, dealerZeroReplayRecordings.dateSeed, dealerZeroReplayRecordings.scenario], set: { eventStream, receiptSnapshot: receipts.map(normalizeReceipt), createdAt: new Date() } }).returning({ id: dealerZeroReplayRecordings.id });
finnor-os/packages/orchestration/src/dealer-zero-replay.ts:    const baseline = recording.receiptSnapshot as ReceiptLike[];
finnor-os/packages/orchestration/src/runtime-bridge.ts:// DecisionReceipt as of §2.4 — a receipt on every action, per the exit gate), but calls
finnor-os/packages/orchestration/src/runtime-bridge.ts:  // validation. This stops a forged SQL status from creating a command, receipt, or
finnor-os/packages/orchestration/src/runtime-bridge.ts:  // actually re-run the plugin, not be swallowed as "already ran". A test proved this
finnor-os/packages/orchestration/src/runtime-bridge.ts:  // this bridge. So every call here is honestly recorded as its own command/step/receipt:
finnor-os/packages/orchestration/src/runtime-bridge.ts:  // one receipt per real attempt, not one receipt straining to cover several.
finnor-os/packages/orchestration/src/runtime-bridge.ts:    // corrupting the receipt AND starving Reflection of the one signal it needs to
finnor-os/packages/orchestration/src/runtime-bridge.ts:    // avoid retrying something that can never succeed (or, as here, to actually retry
finnor-os/packages/orchestration/src/runtime-bridge.ts:    // this fix, classifyFailure()'s output only ever reached the receipt/step row;
finnor-os/packages/orchestration/src/prediction-diff.ts:export interface PredictionDiffField { path: string; predicted: unknown; actual: unknown; matched: boolean }
finnor-os/packages/orchestration/src/prediction-diff.ts:export function diffPrediction(expectedResult: Record<string, unknown> | undefined, actual: Record<string, unknown>): PredictionDiff {
finnor-os/packages/orchestration/src/prediction-diff.ts:    const actualValue = atPath(actual, path);
finnor-os/packages/orchestration/src/prediction-diff.ts:    return { path, predicted, actual: actualValue, matched: JSON.stringify(predicted) === JSON.stringify(actualValue) };
finnor-os/packages/orchestration/src/reflection.ts:// Reflection (§9): compare expected vs actual outcome. On mismatch: retry once, then
finnor-os/packages/orchestration/src/index.ts:    // (real rows start at 1, migration 0023's default), so a receipt citing version 0
finnor-os/packages/orchestration/src/index.ts:    // is honestly distinguishable from one that cites an actual configured policy.
finnor-os/packages/orchestration/src/index.ts:   *  400ms poll is what makes the UNDERSTOOD/PLAN blocks stream in as this actually
finnor-os/packages/orchestration/src/index.ts:      // Real counts from what handleInstruction actually retrieved before planning —
finnor-os/packages/orchestration/src/index.ts:        // drafting, not at doing) — awaitingApproval is what actually distinguishes
finnor-os/packages/orchestration/src/index.ts:    // reads policyId straight off that row, so every system-originated receipt's
finnor-os/packages/orchestration/src/index.ts:   * B2.T6: a terminal runtime receipt is a new planning input, never an excuse to
finnor-os/packages/orchestration/src/index.ts:    const [sourceAction, receipt] = await withTenant(tenantId, async (db) => {
finnor-os/packages/orchestration/src/index.ts:    if (!sourceAction?.planId || !receipt) return;
finnor-os/packages/orchestration/src/index.ts:    // Failed step receipts store their terminal classification in `failure`, while
finnor-os/packages/orchestration/src/index.ts:    // successful ones store data in `actualResult`; keep that receipt shape intact
finnor-os/packages/orchestration/src/index.ts:    const failure = (receipt.failure ?? null) as Record<string, unknown> | null;
finnor-os/packages/orchestration/src/index.ts:    const terminalReceipt = { failure, actualResult: receipt.actualResult ?? null };
finnor-os/packages/orchestration/src/index.ts:        await appendEpisode(tenantId, domainActionId, "plan_repair", { receipt: terminalReceipt }, { status: "no_remainder", sourcePlanId: sourceAction.planId });
finnor-os/packages/orchestration/src/index.ts:      await appendEpisode(tenantId, domainActionId, "plan_repair", { receipt: terminalReceipt, unfinishedRemainder: remainder }, { sourcePlanId: sourceAction.planId, repairPlanId, actionIds: repaired.map((action) => action.id) });
finnor-os/packages/orchestration/src/compiler.ts://     the actual steps for a workflow action (e.g. whether workflow 3 needs a
finnor-os/packages/orchestration/src/compiler.ts:// plugins actually use for a foreign reference. Anything not listed falls through to
finnor-os/packages/orchestration/src/compiler.ts: * to ground entities. This is the one that actually runs the verification queries.
finnor-os/packages/orchestration/src/planner.ts:      "CRITICAL: a prior turn with awaitingApproval:true has NOT actually happened yet — it is a draft sitting in the confirmation queue, nothing was created, and it has no real id of its own kind (e.g. a pending create_invoice has no real invoice id — only a domain_action id, which is a different thing and must never be used as an invoiceId/visitId/etc.). If the current instruction depends on something from a turn that was awaitingApproval:true (e.g. \"remind him about that invoice\" when the invoice draft is still pending), do NOT invent or reuse an id — instead route to answer_business_question explaining that the prior action needs approval first, or ask for the missing identifier some other real way (phone/name lookup).",
finnor-os/packages/orchestration/src/planner.ts:      "If the instruction is a QUESTION about the business (revenue, financial totals, a specific customer's history, trends, anything informational) and no narrower action_type fits exactly, route it to answer_business_question with the verbatim question as payload — that action queries real data across every domain (invoices, leads, inventory, visits, communications history) and answers honestly from whatever is actually there, including saying so when a specific figure isn't tracked. Prefer it over returning empty for any business QUESTION.",
finnor-os/packages/orchestration/src/planner.ts:    // data actually feed back into a real decision instead of sitting inert.
finnor-os/packages/orchestration/src/planner.ts:    // that action. The durable replacement is an advisory manual-step receipt.
finnor-os/packages/orchestration/src/planner.ts:    // every other plugin falls back to an explicitly limited schema prediction.
finnor-os/packages/orchestration/src/planner.ts:    // when repair actually ran (medium/high tiers — low tier has no verdict to
finnor-os/packages/orchestration/src/planner.ts:    // actually fire, and at what tier" signal later phases need.
finnor-os/packages/orchestration/src/policy-simulation.ts:// B6.T2: reports a candidate policy's counterfactual gate effect without executing
finnor-os/packages/orchestration/src/policy-simulation.ts:  const receipts = await withTenant(tenantId, (db) => db.select().from(decisionReceipts).where(and(eq(decisionReceipts.tenantId, tenantId), gte(decisionReceipts.createdAt, since))));
finnor-os/packages/orchestration/src/policy-simulation.ts:  const matching = receipts.filter((receipt) => (receipt.proposedAction as { actionType?: unknown }).actionType === actionType);
finnor-os/packages/orchestration/src/policy-simulation.ts:  const historicalGated = matching.filter((receipt) => (receipt.approval as { required?: unknown }).required === true).length;
finnor-os/packages/orchestration/src/plan-dag.ts:// truth and each eventual execution still creates its ordinary runtime receipt. This
finnor-os/packages/orchestration/src/plan-dag.ts:import { diffPrediction } from "./prediction-diff";
finnor-os/packages/orchestration/src/plan-dag.ts:/** Persist a one-time, field-level prediction comparison once execution actually ran. */
finnor-os/packages/orchestration/src/plan-dag.ts:    db.select({ predictedReceipt: domainActions.predictedReceipt, predictionDiff: domainActions.predictionDiff }).from(domainActions).where(and(eq(domainActions.tenantId, action.tenantId), eq(domainActions.id, action.id))),
finnor-os/packages/orchestration/src/plan-dag.ts:  if (!row?.predictedReceipt || row.predictionDiff) return;
finnor-os/packages/orchestration/src/plan-dag.ts:  const receipt = row.predictedReceipt as { simulation?: { predicted?: { expectedResult?: Record<string, unknown> } } };
finnor-os/packages/orchestration/src/plan-dag.ts:  const diff = diffPrediction(receipt.simulation?.predicted?.expectedResult, result.output);
finnor-os/packages/orchestration/src/plan-dag.ts:  await withTenant(action.tenantId, (db) => db.update(domainActions).set({ predictionDiff: diff }).where(and(eq(domainActions.tenantId, action.tenantId), eq(domainActions.id, action.id))));
finnor-os/packages/orchestration/src/plan-dag.ts:  await appendEpisode(action.tenantId, action.id, "prediction_diff", { predicted: receipt.simulation?.predicted?.expectedResult ?? null, actual: result.output }, { ...diff });
finnor-os/apps/api/app/api/receipts/route.ts:// GET /api/receipts — lookup decision receipts by domainActionId, workflowStepId, or
finnor-os/apps/api/app/api/receipts/route.ts:// live run timeline both need to resolve "their" receipt without knowing its own id
finnor-os/apps/api/app/api/receipts/route.ts:// up front). Tenant-scoped; any signed-in role may read (a receipt is the record of
finnor-os/apps/api/app/api/receipts/route.ts:    return Response.json({ receipts: rows });
finnor-os/apps/api/app/api/receipts/[id]/route.ts:// GET /api/receipts/:id — the full DecisionReceipt for the Phase 7 "Why?" view
finnor-os/apps/api/app/api/receipts/[id]/route.ts:// actual, failure + recovery path. Tenant-scoped, any signed-in role may read.
finnor-os/apps/api/app/api/receipts/[id]/route.ts:    // jarvis-v3 P4.T1: predicted<->actual (§6⑦) lives one hop away from the
finnor-os/apps/api/app/api/receipts/[id]/route.ts:    // receipt itself — predictedReceipt/predictionDiff are columns on the
finnor-os/apps/api/app/api/receipts/[id]/route.ts:    // domain_action this receipt's workflow_step belongs to, not on
finnor-os/apps/api/app/api/receipts/[id]/route.ts:    // decision_receipts. Additive: a receipt with no domainActionId (a rare
finnor-os/apps/api/app/api/receipts/[id]/route.ts:    // simulate() prediction, yields predicted:null/predictionDiff:null — the
finnor-os/apps/api/app/api/receipts/[id]/route.ts:    // frontend's own "No prediction was recorded for this action." variant,
finnor-os/apps/api/app/api/receipts/[id]/route.ts:    let predictionDiff: unknown = null;
finnor-os/apps/api/app/api/receipts/[id]/route.ts:          .select({ predictedReceipt: domainActions.predictedReceipt, predictionDiff: domainActions.predictionDiff })
finnor-os/apps/api/app/api/receipts/[id]/route.ts:        predictionDiff = action.predictionDiff ?? null;
finnor-os/apps/api/app/api/receipts/[id]/route.ts:    return Response.json({ receipt: { ...row, predicted, predictionDiff } });
finnor-os/apps/api/app/api/insights/route.ts:// GET /api/insights — real observability over what the system has actually done:
finnor-os/apps/api/app/api/instructions/[id]/route.ts:// same shape as receipts/[id]/route.ts's own dynamic-id GET.
finnor-os/apps/api/app/api/dealer-zero/time-compression/route.ts:    // D8's pause-and-inspect surface must resolve to actual tenant receipts, not
finnor-os/apps/api/app/api/dealer-zero/time-compression/route.ts:    // current Dealer Zero receipt ids are only attached to its three inspectable
finnor-os/apps/api/app/api/dealer-zero/time-compression/route.ts:    const receipts = await withTenant(ctx.tenantId, (db) =>
finnor-os/apps/api/app/api/dealer-zero/time-compression/route.ts:    let receiptIndex = 0;
finnor-os/apps/api/app/api/dealer-zero/time-compression/route.ts:        if (frame.kind === "day_start" || frame.kind === "day_end") return { ...frame, receiptId: null };
finnor-os/apps/api/app/api/dealer-zero/time-compression/route.ts:        const receiptId = receipts.length > 0 ? receipts[receiptIndex++ % receipts.length]!.id : null;
finnor-os/apps/api/app/api/dealer-zero/time-compression/route.ts:        return { ...frame, receiptId };
finnor-os/apps/api/app/api/activity/route.ts:    // lookup joins in the 2 fields the Activity Theater's feed context actually
finnor-os/apps/api/app/api/activity/route.ts:    // needs to reuse the SAME renderer approvals/receipts already use, instead of
finnor-os/apps/api/app/api/dispatch/map/route.ts:// receipt. Missing coordinates and absent optimizer evidence remain visible as such.
finnor-os/apps/api/app/api/dispatch/map/route.ts:      const receiptRows = await db.select({ actualResult: decisionReceipts.actualResult, payload: domainActions.payload })
finnor-os/apps/api/app/api/dispatch/map/route.ts:      return { isDealerZero: settings?.isDealerZero ?? false, stops, receiptRows, technicians: techniciansForTenant };
finnor-os/apps/api/app/api/dispatch/map/route.ts:    for (const row of data.receiptRows) {
finnor-os/apps/api/app/api/dispatch/map/route.ts:      const result = row.actualResult as { output?: RouteOutput } | null;
finnor-os/apps/api/app/api/data-quality/findings/[id]/resolve/route.ts:// resolution — only a human who has actually checked which value is correct can
finnor-os/apps/api/app/api/setup/status/route.ts:    // real binding — so this report can never silently drift from what actually
finnor-os/apps/api/app/api/admin/migrate/route.ts:    // actually invoked. Runs against the SAME migrations-capable connection as
finnor-os/apps/api/app/api/policies/[tenantId]/[actionType]/route.ts:            // from whatever's actually stored, regardless of what body.data.version says.
finnor-os/apps/api/app/api/integrations/status/route.ts:// don't actually work (expired token, wrong scope, wrong account id) — exactly the
finnor-os/apps/api/app/api/integrations/status/route.ts:    // Which binding actually serves each capability right now (A3.T1: tenant-row ->
finnor-os/apps/api/app/api/integrations/status/route.ts:    // pick the real binding — this report can't drift from what actually executes).
finnor-os/apps/api/app/api/actions/route.ts:    // actually reflects the env at call time, same convention as rate-limit.ts's own
finnor-os/apps/api/app/api/actions/[id]/reject/route.ts:// actual status flip live in FinnorOrchestrator.decide() — shared with confirm and the
finnor-os/apps/api/app/api/actions/[id]/confirm/route.ts:// Audit-first ordering (§19) and the actual approve/execute logic live in
finnor-os/apps/api/app/api/actions/pending/route.ts:// action_log rows when one has actually run (needs AWS_BEDROCK_API_KEY — unconfigured
finnor-os/apps/api/app/api/actions/pending/route.ts:    // A domain action can have more than one receipt (each reflection-retry step opens
finnor-os/apps/api/app/api/actions/pending/route.ts:    const receiptByActionId = new Map<string, ReceiptSummary>();
finnor-os/apps/api/app/api/actions/pending/route.ts:      const receiptRows: ReceiptSummary[] = await withTenant(ctx.tenantId, (db) =>
finnor-os/apps/api/app/api/actions/pending/route.ts:      for (const r of receiptRows) {
finnor-os/apps/api/app/api/actions/pending/route.ts:        if (r.domainActionId && !receiptByActionId.has(r.domainActionId)) receiptByActionId.set(r.domainActionId, r);
finnor-os/apps/api/app/api/actions/pending/route.ts:      // pattern as the receipt lookup above.
finnor-os/apps/api/app/api/actions/pending/route.ts:      receipt: receiptByActionId.get(r.id) ?? null,
finnor-os/apps/api/app/api/actions/pending/route.ts:      // jarvis-v3 P4.T1: the plugin's own simulate() prediction, normalized out of
finnor-os/apps/api/app/api/actions/pending/route.ts:      // ran for this action type — never a fabricated prediction.
finnor-os/apps/api/app/api/documents/[id]/route.ts:// "artifacts retrievable" isn't just a claim: this serves the actual PDF bytes
finnor-os/apps/api/app/api/overview/route.ts:// ungated `get_business_overview` domain action through the normal receipted runtime
finnor-os/apps/api/app/api/overview/route.ts:// bypasses the receipt/citation contract.
finnor-os/apps/api/app/api/overview/route.ts:// seconds, and a pure read producing a brand-new domain_action + decision_receipt row
finnor-os/apps/api/app/api/overview/route.ts:// Old receipts created before the overview contract settled can have an actual result
finnor-os/apps/api/app/api/overview/route.ts:// without the top-level dashboard fields. They are valid historical receipts, but not
finnor-os/apps/api/app/api/overview/route.ts:// fresh, receipted overview through the normal read-only action.
finnor-os/apps/api/app/api/overview/route.ts:        const [receipt] = await withTenant(ctx.tenantId, (db) =>
finnor-os/apps/api/app/api/overview/route.ts:        if (receipt?.actualResult) {
finnor-os/apps/api/app/api/overview/route.ts:          const actual = receipt.actualResult as { output?: Record<string, unknown> };
finnor-os/apps/api/app/api/overview/route.ts:          if (isOverviewOutput(actual.output)) {
finnor-os/apps/api/app/api/overview/route.ts:            return Response.json({ domainActionId: recent.id, receiptId: receipt.id, cached: true, ...actual.output });
finnor-os/apps/api/app/api/webhooks/payment/route.ts:// real regardless: transport-level via checkAndRecordReceipt (webhook_receipts),
finnor-os/apps/api/app/api/webhooks/payment/route.ts:      const receipt = await checkAndRecordReceipt("stripe", event.id, rawBody);
finnor-os/apps/api/app/api/webhooks/payment/route.ts:      if (receipt === "duplicate") return Response.json({ received: true, duplicate: true });
finnor-os/apps/api/app/api/webhooks/payment/route.ts:    const receipt = await checkAndRecordReceipt("payment_provider", parsed.data.providerEventId, rawBody);
finnor-os/apps/api/app/api/webhooks/payment/route.ts:    if (receipt === "duplicate") return Response.json({ received: true, duplicate: true });
finnor-os/apps/api/app/api/webhooks/esign/route.ts:    const receipt = await checkAndRecordReceipt("docusign", `${envelopeId}:${status}`, rawBody);
finnor-os/apps/api/app/api/webhooks/esign/route.ts:    if (receipt === "duplicate") return Response.json({ received: true, duplicate: true });
finnor-os/apps/api/app/api/webhooks/ghl/route.ts:  const receipt = await checkAndRecordReceipt("ghl", String(eventId), rawBody);
finnor-os/apps/api/app/api/webhooks/ghl/route.ts:  if (receipt === "duplicate") return Response.json({ received: true, duplicate: true });
finnor-os/apps/api/app/api/webhooks/marketing/route.ts:    const receipt = await checkAndRecordReceipt("marketing_conversion", parsed.data.eventId, rawBody);
finnor-os/apps/api/app/api/webhooks/marketing/route.ts:    if (receipt === "duplicate") return Response.json({ received: true, duplicate: true });
finnor-os/apps/api/app/api/webhooks/vapi/route.ts: * deployed line to the same single tenant regardless of who was actually dialed.
finnor-os/apps/api/app/api/webhooks/vapi/route.ts:  // userId/role that every caller on this line got, regardless of who was actually
finnor-os/apps/api/app/api/webhooks/vapi/route.ts:        // to what this call's own finnor_instruct actually drafted, never an
finnor-os/apps/api/app/api/webhooks/vapi/route.ts:    const receipt = await checkAndRecordReceipt("vapi", eventId, rawBody);
finnor-os/apps/api/app/api/webhooks/vapi/route.ts:    if (receipt === "duplicate") return Response.json({ received: true, duplicate: true });
finnor-os/apps/api/app/api/corrections/route.ts:// answer wrong with the correction — receipt-linked so it has real provenance, not a
finnor-os/apps/api/app/api/corrections/route.ts:  receiptId: z.string().uuid(),
finnor-os/apps/api/app/api/corrections/route.ts:function describeReceiptAnswer(actualResult: unknown): string {
finnor-os/apps/api/app/api/corrections/route.ts:  if (!actualResult || typeof actualResult !== "object") return "(no recorded answer)";
finnor-os/apps/api/app/api/corrections/route.ts:  const record = actualResult as Record<string, unknown>;
finnor-os/apps/api/app/api/corrections/route.ts:    const [receipt] = await withTenant(ctx.tenantId, (db) =>
finnor-os/apps/api/app/api/corrections/route.ts:      db.select().from(decisionReceipts).where(eq(decisionReceipts.id, parsed.data.receiptId)),
finnor-os/apps/api/app/api/corrections/route.ts:    if (!receipt) return Response.json({ error: "Receipt not found" }, { status: 404 });
finnor-os/apps/api/app/api/corrections/route.ts:      receiptId: receipt.id,
finnor-os/apps/api/app/api/corrections/route.ts:      question: receipt.objective,
finnor-os/apps/api/app/api/corrections/route.ts:      wrongAnswer: describeReceiptAnswer(receipt.actualResult),
finnor-os/apps/api/lib/predicted-outcome.ts:// jarvis-v3 P4.T1 — extracts a plugin's own simulate() prediction out of a
finnor-os/apps/api/lib/predicted-outcome.ts:// outcome) and GET /api/receipts/[id] (post-execution: the predicted<->actual
finnor-os/apps/api/lib/predicted-outcome.ts:// B2.T2) returns null, never a fabricated prediction.
finnor-os/apps/api/lib/intake-idempotency.ts:// rejected BEFORE the orchestrator ever runs a second time, which is what actually makes
finnor-os/apps/api/lib/webhook-replay.ts:// Webhook replay protection — one row per (provider, event_id) in webhook_receipts
finnor-os/apps/api/lib/webhook-replay.ts:    `INSERT INTO webhook_receipts (provider, event_id, payload_hash) VALUES ($1, $2, $3)
finnor-os/apps/api/lib/auth.ts:  // The driver's real root cause (e.g. drizzle's DrizzleQueryError wraps the actual
src/components/jarvis/ui/renderers/ActionRenderer.tsx:  // genuinely unregistered action type — FallbackRenderer's own raw-JSON view
src/components/jarvis/ui/renderers/SchemaCard.tsx:// same posture `FallbackRenderer` documented for itself). Spec: plugin-family
src/components/jarvis/ui/renderers/SchemaCard.tsx:// FallbackRenderer -> owner-debug only (closes DEFECT LEDGER NEW-8): raw
src/components/jarvis/ui/renderers/SchemaCard.tsx:import { FallbackRenderer } from "./FallbackRenderer"
src/components/jarvis/ui/renderers/SchemaCard.tsx:              <FallbackRenderer actionType={actionType} payload={payload} />
src/components/jarvis/ui/renderers/FallbackRenderer.tsx:export function FallbackRenderer({ actionType, payload, compact }: ActionRendererProps) {
src/components/jarvis/ui/renderers/registry.ts:// FallbackRenderer.tsx (a designed, debug-gated backstop) is the only path that can
src/components/jarvis/ui/renderers/registry.ts:export const REGISTERED_ACTION_TYPES: string[] = Object.keys(ACTION_RENDERERS)
src/components/jarvis/ui/renderers/RendererCatalog.tsx:import { ACTION_RENDERERS, REGISTERED_ACTION_TYPES } from "./registry"
src/components/jarvis/ui/renderers/RendererCatalog.tsx:        <span className="j-chip bg-cyan-400/12 text-cyan-300">{REGISTERED_ACTION_TYPES.length} / 42 registered</span>
src/components/jarvis/ui/renderers/RendererCatalog.tsx:          {REGISTERED_ACTION_TYPES.map((actionType) => (
src/components/jarvis/ui/renderers/ClarificationScene.tsx:// `ActionRenderer` fell through to `FallbackRenderer` — the amber "unmapped

P0.T6 final CI-equivalent command matrix summary

PASS, exit 0:
  root: npm run lint; npm run test:unit; npm run build; node scripts/contrast-audit.mjs
  backend: npm run authz:matrix:check; npm run typecheck; npm run test:planner-evals;
    npm run policy:lint; fresh db:migrate; fresh db:seed; setup:langgraph; npm test
  release: npm run release:manifest (44/44); npm run release:environment (144 names, no values)
  backup: npx tsx scripts/backup-restore-drill.ts (row-count round-trip PASS)
  security: current-diff gitleaks; OSV lockfile scan; npm audit
  browser: 280 total, 113 passed, 167 pre-existing skips, 0 failures

  local staging guard repeat: exact STAGING=1 test:staging against a fresh
  disposable local database had one first-run exit 1 (one planner-DAG PostgreSQL
  connection-timeout failure), followed by three fresh exit-0 reruns with 121 passed
  test files, 1 pre-existing skipped file, 557 passed tests, and 3 pre-existing
  skipped tests. Full timings and cleanup evidence: p0-t6-local-staging-guard-repeat.txt.

Expected fail-closed/environment-gated results, recorded rather than bypassed:
  npm run test:staging — refuses without explicit STAGING=1 and verified staging DATABASE_URL;
    STAGING=1 without DATABASE_URL also refuses. The local exact command was exercised against
    disposable Postgres and is recorded above; it is not remote staging certification. A current
    Vercel Preview DATABASE_URL was
    discovered read-only, but its PostgreSQL handshake resets (`ECONNRESET`) and its Supabase
    auth origin matches Production; the staging suite was not pointed at that unsafe/unavailable
    target. See p0-t7-vercel-preview-audit.txt.
  npx tsx scripts/probe-tenant-isolation.ts — refuses without both environment URLs, two JWTs,
    and tenant-A marker IDs.
  npx tsx scripts/replay-dealer-zero.ts — refuses without baseline/candidate staging artifacts.
  k6 — not installed locally; the workflow requires a staging URL, tenant, token, and webhook
    secret before running the CI-only load scenario.
  planner live eval — current Preview credential metadata is present, but the provider-backed run
    completed 11/41 passed, 29 failed, 1 errored (exit 1). The workflow had only a Groq secret while
    the default planning/repair routes exclude Groq; a CI-only route pin was added, but a follow-up
    diagnostic hit the configured Groq TPM limit and was stopped after confirming the failure. See
    p0-t6-planner-live-eval.txt. No passing live-eval claim is made.

No production deployment, migration, provider call, or live outbound effect was performed.

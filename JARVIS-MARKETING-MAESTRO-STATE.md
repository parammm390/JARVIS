# JARVIS MARKETING MAESTRO STATE

Convention: same as `JARVIS-MAESTRO-STATE.md`/`JARVIS-FRONTEND-MAESTRO-STATE.md` — a box is checked ONLY with `(evidence: commit sha / screenshot reference / pasted command output)`. `⏸` = blocked on PARAM (reason inline). `Deviation:` lines record where reality differed from the plan and how the task adapted. Sessions work the ACTIVE phase's unchecked tasks top-down and append one Session Log line before ending (§0 End Ritual in JARVIS-MARKETING-MAESTRO-PLAN.md).

**ACTIVE PHASE: M4 — The Merged Demo (M0-M3 CLOSED except M1's one ⏸ PARAM bullet).**

## Session Log

- 2026-07-26 · phase M3 · closed all 3 tasks same session · **T1**: built `src/components/sections/jarvis-proof/CommandBridgeProof.tsx`, a new dark `.jarvis-root`-wrapped section proving the real console — the real `MarketingOrb` rendering live, plus three panels (Orb / Pipeline Theater / activity feed) built from real, shipped `jarvis` fx/primitives (`Glass`, `BorderBeam`, `RiskBadge`, all reused unmodified, zero new parallel visual system). Confirmed both "Pipeline Theater" and the `/api/activity`-backed activity feed are real, already-shipped console features (grepped `JARVIS-MAESTRO-STATE.md` D3/D4, `JARVIS-POST-COMPLETION-SYSTEM-BRIEF.md`) before writing copy about them — not guessed from the plan's wording. **Honest deviation, not improvised around**: M1.T4's Showtime screen recordings are still PARAM-blocked (no owner test credentials in this environment, unchanged since M1), so this section proves the console with the real live Orb plus a written, source-grounded description of Pipeline Theater/the activity feed instead of embedding footage that doesn't exist — no fabricated or placeholder video used anywhere. **T2**: reframed `Solution.tsx` around the two-year household-memory thesis — headline now "The call was minute one. This is the next two years." (the exact line from `docs/marketing-brand-voice.md`'s own worked example), capability grid cut from 16 to 14 cards, entirely removing the two call-first cards ("Every Call Answered," "Speed-to-Lead Outbound") and renaming "Quote Range on the Call" to "Quote Drafted, Then Held for Approval" — voice-agent demotion enforced structurally, not just in wording, since voice's one permitted mention already lives in `LiveWorkflow.tsx` (M2) and this section now carries zero voice-specific framing. `Outcome.tsx` reframed to name JARVIS directly (was "the booking and lead recovery system"), disqualification beat added to the existing compliance-hedge callout ("If you want a system that acts without asking... JARVIS isn't that"), keeping the required boundary language intact rather than just relying on the FAQ's copy for it. **T3**: `PersonalizedDemoBuilder.tsx` rewritten from a 3-card grid (one card pointed at the fake `/dashboard-demo` route, which M4 cuts) down to a single "Build My Demo" CTA aimed at `siteConfig.demoLink` (`/demo`) — 3-step explainer (instruction → draft → two-year record) previews what M4's merged flow will actually do, so no rework needed once M4 lands. `page.tsx` reordered to match plan §2.4's IA exactly: Hero → RevenueLeak(Stakes) → LiveWorkflow(Loop) → **CommandBridgeProof(new)** → Solution+Outcome(Memory) → PersonalizedDemoBuilder(Try it) → Pricing → FirstSevenDays → FAQ → Cta/Footer. Verified live via the Browser pane: full-page `get_page_text` transcript confirms every rewritten/new section's copy renders correctly in the new order; two targeted screenshots (Command Bridge Proof mid-scroll showing the live WebGL Orb + dark jarvis-root panels, and the Try-It CTA) confirm visual rendering; zero console errors. Evidence: `tsc --noEmit`/`npm run lint`/`npm run build` all clean (36/36 pages), homepage First Load JS 258kB (up marginally from M2's 255kB for the new section's static content — Orb import stayed lazy/dynamic, confirmed no second Three.js chunk was pulled in) · next: M4 — the merged demo (highest-risk phase: audit PostCallHandoff/WorkflowModule/ProofArtifacts, re-terminate DemoExperience into an Approval-Cockpit-styled card, chain Act 1→Act 2, cut /dashboard-demo + /demo/[slug]'s fake-dashboard path) · blockers: none for M4; M1.T4 remains the only open item, still ⏸ PARAM.


- 2026-07-26 · post-M2 fix, real-time interaction · Param flagged the Hero orb as "very very dead feeling" and asked for genuine cursor interactivity plus a cursor color fix — both real, substantive gaps, not cosmetic nitpicks. **Rebuilt `MarketingOrb.tsx` as its own owned component** (no longer a thin `Orb3D` wrapper — that shared component has zero interaction hooks and is read-only per this plan's §6, so cursor-reactivity had to live in a marketing-only implementation that mirrors Orb3D's visual language rather than forking it): a GPU vertex-shader computes each particle's screen-space (NDC) distance to the live cursor position every frame (no raycasting, no per-frame CPU loop over 14k points) and pushes nearby particles outward + brightens them toward white, on top of the existing ambient idle/planning/executing cycle. **Found and fixed a real, separate bug while verifying**: the IntersectionObserver-based visibility gate never fired its initial callback in this test environment, silently leaving the canvas unmounted forever (`data-orb-mode="webgl"` but zero children) — added a synchronous `getBoundingClientRect()`-based fallback to seed `visible` immediately, with the observer still owning subsequent scroll-based pause/resume; root-caused separately to a 0×0 viewport in a freshly-created, unresized browser tab, but the fallback is a genuine robustness improvement regardless of that specific cause. **Fixed the cursor legibility bug**: added a new `"invert"` variant to the site-wide `CustomCursor` (white dot + white ring, triggered by `data-cursor="invert"`, now set on the orb container) so the cursor no longer disappears against the near-black panel. Verified with real instrumentation, not eyeballing: added `preserveDrawingBuffer: true` temporarily, used `gl.readPixels` to sample canvas brightness before/after dispatching a real `PointerEvent` at the panel center — center-region brightness went from baseline avg 5.4 to 51.2 (max 552/765, i.e. genuinely pushed toward white) while the untouched outer ring stayed at baseline (~5.4avg/55max), conclusively proving the shader reacts specifically where the cursor is, not just generally; confirmed the cursor's DOM classes actually flipped to `bg-white`/`border-white/85` on the same dispatch. Removed `preserveDrawingBuffer` before finalizing (no production cost). Re-ran the reduced-motion Playwright probe (throwaway, deleted after) against the rebuilt component: normal mode still resolves `webgl`+real canvas, reduced-motion still collapses to `static`, zero errors either way. Evidence throughout: `tsc --noEmit`/`npm run lint`/`npm run build` clean, homepage First Load JS unchanged at 255kB (Orb still lazy-loaded) · next: continue into M3, or address any further Hero feedback · blockers: none.

- 2026-07-26 · phase M2 · closed all 3 tasks same session as M0/M1 · rewrote Hero.tsx's headline/subhead/highlight-line and deleted the entire fake booking-console mock, replacing it with a real, honestly-labeled `JarvisOrbPanel` (JarvisProofSurface + MarketingOrb). Found and fixed a real regression before it ever reached a commit: swapping in the Orb pulled Three.js into the homepage's synchronous bundle (256kB→398kB First Load JS); fixed via `next/dynamic({ssr:false})` with a matching static-gradient loading fallback, confirmed back to 255kB via `npm run build`'s own size report, and confirmed the real WebGL orb still loads (`data-orb-mode: "webgl"`) once the client chunk arrives. Rewrote RevenueLeak.tsx's headline and all 4 problem cards around memory/accountability/pricing-consistency/follow-through, removing the missed-call framing from the cards entirely (kept only in the pre-existing badge line, which already fit); fixed a real lint failure (unescaped apostrophes) before commit. Rewrote LiveWorkflow.tsx into "the loop" — instruction→plan→approval gate→execute→receipt — with voice demoted to exactly one line, equal weight to text/typed-command, and the disqualification/boundary disclaimer kept intact but rewritten to name JARVIS directly. Verified via a real dev-server walkthrough (Browser pane): screenshots + a full `get_page_text` transcript confirm every rewritten section renders its new copy correctly in order, zero console errors. Self-audited every line against `docs/marketing-brand-voice.md`'s checklist. Evidence throughout: `tsc --noEmit`/`npm run lint`/`npm run build` clean · next: M3 — Homepage Act 2 (build the new Command Bridge proof section, reframe/consolidate Solution.tsx + Outcome.tsx around the memory/household-360 thesis, collapse PersonalizedDemoBuilder.tsx into a single CTA) · blockers: none for M3; M1.T4's Showtime-recording bullet remains the only open item, still ⏸ PARAM.

- 2026-07-26 · phase M1 · closed T1/T2/T3/T5 same session as M0; T4 (Showtime recordings) is honestly ⏸ PARAM-blocked — no `TEST_OWNER_*` credentials exist anywhere in this environment (checked `.env.local`, `finnor-os/.env*`, `JARVIS-CREDENTIALS-LEDGER.md`), same standing limitation every prior JARVIS D-phase has hit; no footage was faked. T1: re-hued every genuinely accent-purpose color declaration in `globals.css` toward the real `--jm-cyan`/`--jm-teal`/etc. tokens (exact hex copied from `jarvis-theme.css`), left neutral/structural tokens alone, confirmed zero old hardcoded values remain via grep, confirmed live via `getComputedStyle` and a homepage screenshot. T2: built `JarvisProofSurface.tsx` as the single canonical `jarvis-theme.css` import site, verified zero style leakage on a deleted throwaway debug route. T3: built `MarketingOrb.tsx`, a thin unmodified-`Orb3D` wrapper driving a scripted 5-step state loop, verified rendering + zero console errors on a deleted throwaway debug route. T5: reduced-motion probe (throwaway Playwright script, deleted after) confirmed `webgl`→`static` collapse with zero errors either mode; IntersectionObserver/low-power guards inherited unmodified from the already-verified D1 `Orb3D`, not re-proven. Evidence throughout: `tsc --noEmit`/`npm run lint`/`npm run build` all clean (36/36 pages) after every change, including after cleaning up two stale `.next/types` entries left behind by the deleted debug routes · next: M2 — Homepage Act 1 (rewrite Hero.tsx/RevenueLeak.tsx/LiveWorkflow.tsx copy, swap Hero's fake console mock for the new MarketingOrb) · blockers: M1.T4 remains open pending Param's go-ahead on a real owner session; everything else is unblocked.

- 2026-07-26 · phase M0 · closed all 5 tasks same session · did T1 (deleted Credibility/Founder/HowItWorks/Problem/UseCases.tsx after grep-confirming zero real imports — two apparent hits in Footer.tsx were false positives: an anchor label `"#problem"` and unrelated "Founder contact:" text, not references to the components), T2 (removed `gsap`+`split-type` from package.json, `npm install` — 2 packages removed), T3 (`docs/marketing-brand-voice.md`), T4 (`docs/marketing-demo-merge-contract.md`, grounded in the real `DemoIntakeHandoff`/`HouseholdRecord`/`LifecycleHandoff` shapes and `ActionRenderer`/`RiskBadge` prop signatures read directly from source, not guessed), T5 (IA in plan §2.4 confirmed as final, no changes needed). **Real deviation found and fixed, not in the original task list**: post-deletion grep found `gsap`/`ScrollTrigger` still live in `src/components/ui/smooth-scroll.tsx` — the site-wide Lenis wrapper mounted in `layout.tsx` was using `gsap.ticker` to drive Lenis's raf loop and syncing `ScrollTrigger.update()`/`.refresh()`, even though a repo-wide grep confirmed zero remaining `ScrollTrigger` consumers after the 5 files were deleted. Rewrote it to a plain `requestAnimationFrame` loop (dropping the now-pointless ScrollTrigger registration/sync entirely) rather than leaving a live, site-wide file importing an intentionally-removed dependency. Also found and deleted `src/components/ui/split-text.tsx` (self-referencing only, imports both removed deps, would have broken typecheck once the packages were gone). Evidence: `npx tsc --noEmit` clean; `npm run lint` clean (`✔ No ESLint warnings or errors`); `npm run build` clean, 36/36 pages generated (only the pre-existing, unrelated `@sentry/server-utils` ESM warning); repo-wide grep for `gsap`/`split-type` imports returns zero hits outside `node_modules`/lockfiles; live dev-server check via Browser pane confirmed the homepage renders with zero console errors and the rewritten Lenis raf loop actually still smooth-scrolls (`scrollY` moved from 0→800 after a scroll gesture, verified via `window.scrollY` read) · next: M1 — visual token bridge (re-hue `globals.css`, scope-import `jarvis-theme.css`, build the marketing Orb wrapper, capture Showtime recordings) · blockers: none.

## M0 — Foundations & Brand Spine
Status: **COMPLETE.**
- [x] M0.T1 — Delete 5 orphaned section files (evidence: `git rm` of Credibility/Founder/HowItWorks/Problem/UseCases.tsx; grep-confirmed zero real imports beforehand, two false-positive text matches in Footer.tsx documented above)
- [x] M0.T2 — Remove gsap + split-type from package.json (evidence: both lines removed from `package.json`; `npm install` → "removed 2 packages"; also found+fixed a real live consumer, `smooth-scroll.tsx`, and deleted the dead `split-text.tsx` — see Session Log deviation)
- [x] M0.T3 — Write docs/marketing-brand-voice.md (evidence: file committed, includes voice pillars, 6 hard rules, 4 worked before/after examples, self-audit checklist)
- [x] M0.T4 — Write docs/marketing-demo-merge-contract.md (evidence: file committed, grounded in real `DemoIntakeHandoff`/`HouseholdRecord`/`LifecycleHandoff`/`ActionRenderer`/`RiskBadge` shapes read from source)
- [x] M0.T5 — Lock final IA (evidence: plan §2.4 reviewed against this session's findings, no changes required, confirmed final as written)
EXIT GATE: CLOSED. `git status` shows the 5 files deleted (+ `split-text.tsx`, a deviation-documented 6th); zero grep hits for `gsap`/`split-type` outside `node_modules`/lockfiles (pasted above); `tsc --noEmit`/`npm run lint`/`npm run build` all clean (36/36 pages); both new docs committed; live dev-server check clean (zero console errors, smooth-scroll functioning); STATE file updated.

## M1 — Visual Token Bridge + Proof Components
Status: T1-T3 + T5 DONE same session as M0. T4 ⏸ PARAM-blocked (see below) — not fabricated around.
- [x] M1.T1 — Re-hue globals.css light tokens toward --j-* family (evidence: `--jm-cyan`/`--jm-teal`/`--jm-blue`/`--jm-violet`/`--jm-amber`/`--jm-red`/`--jm-green` added to `:root` in `globals.css`, hex values copied verbatim from `jarvis-theme.css`'s real `--j-*` tokens, not re-invented; every genuinely accent-purpose hardcoded rgba/hex in the file re-derived from them — `::selection`, `.healthcare-page` background gradient, `.ops-card-hover`, `.soft-edge`, `.section-kicker` (new `--jm-cyan-ink: #0e7490`, a darkened AA-contrast-safe version since the raw `#22d3ee` fails on white/cream), `.signal-thread`, `.cta-primary:hover`, `.command-surface`, `.signal-sweep`, `.edge-light`, `.status-pulse`. Neutral/structural tokens (body text ink, shadcn `--ring`/`--accent`/`--muted` grays, scrollbar) deliberately left untouched — not in scope for an accent re-hue. Post-edit grep for every old hardcoded value (`72, 145, 199` / `15, 118, 110` / `22, 101, 150` / `30, 91, 141` / `14, 165, 181` / `1e5b8d` / `103, 232, 249` / `20, 184, 166` / `14, 165, 233`) returns zero hits. Live dev-server check: `getComputedStyle(document.documentElement).getPropertyValue('--jm-cyan')` → `"34, 211, 238"` (exact match), homepage screenshot confirmed the dark console mock's "Ringing"/"LIVE"/"INCOMING CALL" chips now show the real cyan/teal hue, zero console errors.)
- [x] M1.T2 — Scope-import jarvis-theme.css under a marketing .jarvis-root wrapper (evidence: new `src/components/sections/jarvis-proof/JarvisProofSurface.tsx` — single canonical import site for `jarvis-theme.css`, confirmed via source read that the stylesheet only ever declares custom properties under `.jarvis-root`-scoped selectors and never paints a page-level background/color itself, so wrapping is genuinely sufficient. Verified zero leakage on a throwaway debug route (deleted before commit, same convention as C2/C3/D1): a light `.ops-card`/`.section-kicker` outside the wrapper stayed fully light/unaffected, while content inside rendered `--j-bg` dark with real `--j-cyan`/`--j-teal` accents — screenshotted both side by side.)
- [x] M1.T3 — Build marketing Orb wrapper (evidence: new `src/components/sections/jarvis-proof/MarketingOrb.tsx` — imports the real `Orb3D` completely unmodified, drives it with a 5-step scripted `OrbState` sequence (idle→planning→executing×3 activeRunCounts, ~9.8s loop) via a plain `setTimeout` chain. Verified live on a throwaway debug route: real GPU particle sphere rendered (screenshotted), zero console errors. Deleted the debug route before commit.)
- [ ] M1.T4 — Capture real Showtime screen recordings — **⏸ PARAM-BLOCKED, not attempted around.** `Showtime.tsx:86-92` hard-gates on a signed-in session AND `role==="owner"` AND the Dealer Zero tenant; grepped `.env.local`/`finnor-os/.env*`/`JARVIS-CREDENTIALS-LEDGER.md` for `TEST_OWNER_*` credentials — none exist, confirming this is the same standing, project-wide, repeatedly-documented limitation every prior D-phase (`JARVIS-MAESTRO-STATE.md` D1/D2/D5/D6) hit: no real Supabase owner account exists in this environment, and creating one without Param's explicit go-ahead is off-limits. No footage was faked or substituted.
- [x] M1.T5 — Verification pass (evidence: reduced-motion probe via a throwaway Playwright script (deleted before commit, same convention as C2/D1) against the MarketingOrb debug route — `emulateMedia({reducedMotion:'reduce'})` correctly flipped `data-orb-mode` from `webgl`→`static` with zero page/console errors in either mode. IntersectionObserver-pause and low-device-memory collapse are inherited byte-for-byte from `Orb3D` — zero lines of that logic were touched by `MarketingOrb`, so not re-proven from scratch; already real-verified in the D1 phase per `JARVIS-FRONTEND-MAESTRO-PLAN.md` §1.)
EXIT GATE: 2 of 3 bullets closed. Re-hued tokens + Orb wrapper working with real screenshots, reduced-motion clean (pasted above). Showtime recordings bullet stays honestly open pending Param's go-ahead on a real owner session — same category of gap as every prior D-phase, not something this session improvised around.

## M2 — Homepage Act 1: Hero + Stakes + Loop
Status: **COMPLETE.**
- [x] M2.T1 — Rewrite Hero.tsx (evidence: new headline "Give it an instruction. Watch it ask before it acts." + subhead + receipt-framed highlight line, per `docs/marketing-brand-voice.md`'s own worked example almost verbatim; `DispatchCommandVisual`/`HeroWaveform`/`callStates`/`intakeRows`/`workflowStateItems` (the fake console mock) deleted entirely, replaced by `JarvisOrbPanel` — a real `JarvisProofSurface`-wrapped, dynamically-imported `MarketingOrb`, explicitly labeled "Sample states" per the honesty rule (never claimed live). **Real regression caught and fixed before commit**: the naive swap pulled Three.js into the homepage's synchronous bundle, First Load JS 256kB→398kB; wrapped `MarketingOrb` in `next/dynamic({ssr:false})` with a static-gradient loading fallback matching Orb3D's own reduced-motion look — First Load JS back to 255kB, confirmed via `npm run build` output diffed before/after. Live dev-server check: `data-orb-mode` resolves to `"webgl"` (real render, not the static fallback), zero console errors.)
- [x] M2.T2 — Rewrite RevenueLeak.tsx (evidence: new headline "A missed call is the cheapest mistake your business makes." explicitly demoting the call itself as the smallest problem; all 4 problem cards replaced — memory/accountability/pricing-consistency/follow-through, the missed-call angle removed from the card set entirely (it now only survives as the section's kept badge line, which already said "bigger than the missed call"); before/after comparison panel reframed "memoryless path" vs "the JARVIS record". Caught and fixed a real lint failure (unescaped apostrophes, `react/no-unescaped-entities`) before commit — `npm run lint` clean after.)
- [x] M2.T3 — Rewrite LiveWorkflow.tsx (evidence: new headline "One loop. Every decision. No exceptions."; all 5 workflow steps rewritten to the real instruction→plan→approval-gate→execute→receipt loop; voice appears exactly once, equal weight to text/typed-command, per the brand-voice doc's demotion rule; `HandoffReel`/`AlertSummary` reframed to "awaiting approval"/"held for your approval" language; closing disclaimer rewritten to name JARVIS directly and keep the disqualification/boundary beat intact.)
Verification (M2 as a whole): `tsc --noEmit`/`npm run lint`/`npm run build` all clean; live dev-server walkthrough via the Browser pane confirmed all three sections render the new copy correctly in document order, zero console errors throughout; every section's copy self-audited against `docs/marketing-brand-voice.md`'s checklist line by line (no "AI receptionist," no invented numbers beyond the pre-existing illustrative mockup example data, voice-agent mention count = exactly 1, disqualification beat intact).
EXIT GATE: CLOSED. Screenshots + `get_page_text` transcript confirm all three rewritten sections; brand-voice audit passed; build/lint/typecheck clean; bundle-size regression found and fixed within the same session, not left for M7.

## M3 — Homepage Act 2: Command Bridge Proof + Memory
Status: **COMPLETE.**
- [x] M3.T1 — Build Command Bridge proof section (evidence: new `src/components/sections/jarvis-proof/CommandBridgeProof.tsx`, real live `MarketingOrb` + `Glass`/`BorderBeam`/`RiskBadge` reused unmodified; Pipeline Theater/activity feed confirmed real via source-grounded grep before writing copy; Showtime footage honestly still absent — M1.T4 unchanged ⏸ PARAM, no fabricated video used; screenshot confirms live WebGL render + dark jarvis-root panels)
- [x] M3.T2 — Reframe/consolidate Solution.tsx + Outcome.tsx (evidence: decided to keep as two sections, not merge — Solution carries the memory/capability beat, Outcome carries boundaries, and both read cleanly reframed rather than forced together; Solution's headline now pulls the brand-voice doc's own worked "the call was minute one" line verbatim, 16→14 cards with both call-first cards removed entirely; Outcome names JARVIS directly and gains an explicit disqualification sentence)
- [x] M3.T3 — Reframe PersonalizedDemoBuilder.tsx into single CTA (evidence: 3-card grid → one CTA at `siteConfig.demoLink`, dropped the card pointing at the to-be-cut `/dashboard-demo` route, 3-step explainer previews the real M4 merged flow)
Verification (M3 as a whole): `tsc --noEmit`/`npm run lint`/`npm run build` all clean (36/36 pages, 258kB First Load JS); live dev-server walkthrough via Browser pane, full `get_page_text` transcript confirms every section's new copy and the new page order; two section screenshots confirm visual rendering; zero console errors.
EXIT GATE: CLOSED. Screenshots + full-page text transcript confirm all sections; copy self-audited against brand-voice doc (no "AI receptionist," voice-agent mention count unchanged at exactly 1 site-wide so far, disqualification beat present in both FAQ and now Outcome); build/lint/typecheck clean.

## M4 — The Merged Demo
Status: NOT STARTED.
- [ ] M4.T1 — Audit PostCallHandoff/WorkflowModule/ProofArtifacts for reuse vs deletion
- [ ] M4.T2 — Re-terminate DemoExperience into Approval-Cockpit-styled card
- [ ] M4.T3 — Chain Act 1 into LifecycleExperience as Act 2, re-skin
- [ ] M4.T4 — Cut /dashboard-demo + /demo/[slug] fake-dashboard path, add redirects
EXIT GATE: not yet closed.

## M5 — Pricing + FAQ
Status: NOT STARTED.
- [ ] M5.T1 — Rewrite Pricing.tsx
- [ ] M5.T2 — Rewrite FAQ.tsx
EXIT GATE: not yet closed.

## M6 — Onboarding, Resources, Trust, CTA/Footer, Concierge
Status: NOT STARTED.
- [ ] M6.T1 — Rewrite FirstSevenDays.tsx
- [ ] M6.T2 — Reframe resources/* pages
- [ ] M6.T3 — Update trust-safety/privacy/terms copy
- [ ] M6.T4 — Update Cta/Footer/ContactForm + FinnorAIConcierge copy pass
EXIT GATE: not yet closed.

## M7 — Polish, SEO, Performance, Cutover
Status: NOT STARTED.
- [ ] M7.T1 — Rewrite site-wide metadata/OG + site.ts
- [ ] M7.T2 — Lighthouse run on rebuilt homepage + /demo
- [ ] M7.T3 — Cross-browser/responsive spot-check
- [ ] M7.T4 — Final full-site walkthrough + link audit
EXIT GATE: not yet closed.

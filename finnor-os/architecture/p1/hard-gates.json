{
  "schemaVersion": 1,
  "p1BaselineSha": "8fcd8a1cebcf92791047777c0d9c70e95fc7aad2",
  "p0CertifiedSha": "f21cd6c45c03e177a46019cb768fa0fe26e25a9f",
  "gates": [
    { "id": "existing_routing_regressions", "expected": 0, "evidence": [{ "file": "tests/unit/instruction-routing-policy.test.ts", "title": "preserves deterministic Operational Query Plane reads" }] },
    { "id": "new_execution_models", "expected": 0, "evidence": [{ "file": "packages/operational-ir/src/adapters-lowerer.test.ts", "title": "lowers the audited KNOWN_ACTION_COMPATIBILITY model without creating a new execution model" }] },
    { "id": "new_authority_systems", "expected": 0, "evidence": [{ "file": "packages/operational-ir/src/adapters-lowerer.test.ts", "title": "lowers atomic IR into a draft DomainAction without authority/effect/idempotency identity" }] },
    { "id": "new_work_lifecycle_systems", "expected": 0, "evidence": [{ "file": "packages/operational-ir/src/adapters-lowerer.test.ts", "title": "never reuses the IR hash as DomainAction, Work, BusinessEffect, or idempotency identity" }] },
    { "id": "new_business_effect_identity_domains", "expected": 0, "evidence": [{ "file": "packages/operational-ir/src/adapters-lowerer.test.ts", "title": "projects BusinessEffect only for comparison and never as an IR identity" }] },
    { "id": "ir_hash_used_as_effect_or_idempotency_identity", "expected": 0, "evidence": [{ "file": "packages/operational-ir/src/canonical.test.ts", "title": "uses a tagged identity domain that cannot masquerade as a BusinessEffect hash/idempotency key" }] },
    { "id": "new_package_cycles", "expected": 0, "evidence": [{ "file": "packages/operational-ir/src/import-boundary.test.ts", "title": "keeps shared-types below Operational IR and prevents reverse/transitive cycles" }] },
    { "id": "ir_serialization_nondeterminism", "expected": 0, "evidence": [{ "file": "packages/operational-ir/src/canonical.test.ts", "title": "is invariant to arbitrary JSON object insertion order" }] },
    { "id": "ir_hash_nondeterminism", "expected": 0, "evidence": [{ "file": "packages/orchestration/src/operational-ir-shadow.test.ts", "title": "keeps semantic hash stable across provenance timestamps and ids" }] },
    { "id": "known_malformed_ir_accepted", "expected": 0, "evidence": [{ "file": "packages/operational-ir/src/validation.test.ts", "title": "rejects a schema with a required artifact missing" }] },
    { "id": "known_invalid_dependency_cycles_accepted", "expected": 0, "evidence": [{ "file": "packages/operational-ir/src/validation.test.ts", "title": "rejects forbidden dependency cycles" }] },
    { "id": "shadow_ir_consequential_mutations", "expected": 0, "evidence": [{ "file": "packages/orchestration/src/operational-ir-shadow.test.ts", "title": "uses the same route candidate, validates, lowers exactly, and records EQUIVALENT with zero mutations" }] },
    { "id": "verification_semantics_weakened", "expected": 0, "evidence": [{ "file": "packages/operational-ir/src/validation.test.ts", "title": "rejects a supplemental observation used as an Effect's expected proof" }] },
    { "id": "consequential_execution_bypasses", "expected": 0, "evidence": [{ "file": "packages/orchestration/src/operational-ir-shadow.test.ts", "title": "has no model, provider, authority, persistence, or execution callback seam" }] },
    { "id": "unexplained_semantic_diff_regressions", "expected": 0, "evidence": [{ "file": "packages/operational-ir/src/semantic-shadow.test.ts", "title": "fails a changed Operational Query request as REGRESSION" }] },
    { "id": "p0_invariant_regressions", "expected": 0, "evidence": [{ "file": "tests/unit/p0-locked-replay.test.ts", "title": "locks every required semantic category to an existing non-live test selector and exact corpus hash" }] }
  ]
}

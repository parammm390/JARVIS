{
  "schemaVersion": 1,
  "fixedClock": "2026-09-01T00:00:00.000Z",
  "seed": 6092026,
  "cases": [
    { "id": "identical_successes", "selector": "two semantically identical verified executions align" },
    { "id": "different_entity_ids", "selector": "entity identifiers become parameters" },
    { "id": "different_amounts", "selector": "amount becomes a parameter" },
    { "id": "true_constant", "selector": "repeated public channel literal becomes an evidence-backed candidate constant" },
    { "id": "false_constant", "selector": "varying channel is not inferred constant" },
    { "id": "derived_value", "selector": "derived value retains explicit transform provenance" },
    { "id": "optional_step", "selector": "missing non-safety step remains optional" },
    { "id": "reordered_independent", "selector": "independent reordered steps align by semantics" },
    { "id": "branch_inference", "selector": "two observed arms induce one branch" },
    { "id": "false_branch_inference", "selector": "one observed arm fabricates no branch" },
    { "id": "safe_retry", "selector": "idempotent retry is distinguished from ambiguous mutation" },
    { "id": "reconcile_before_retry", "selector": "ambiguous mutation requires reconciliation before retry" },
    { "id": "loop", "selector": "two traces with bounded iterator evidence induce one loop" },
    { "id": "repeated_not_loop", "selector": "repeated sequence without iterator evidence induces no loop" },
    { "id": "event_wait", "selector": "event wait remains event-driven" },
    { "id": "fixed_wait", "selector": "fixed-duration wait remains fixed" },
    { "id": "human_approval", "selector": "approval requirement survives generalization" },
    { "id": "provider_observation", "selector": "provider implementations align while reality observation survives" },
    { "id": "computer_observation", "selector": "computer page state remains an external observation" },
    { "id": "model_decision", "selector": "model semantic role persists without prompt or chain-of-thought" },
    { "id": "compensation", "selector": "compensation edge and recovered outcome survive" },
    { "id": "recovered_success", "selector": "failed attempt followed by verified recovery is recovered success" },
    { "id": "failure_trace", "selector": "failure is negative evidence rather than positive body" },
    { "id": "ambiguous_outcome", "selector": "provider acknowledgement alone is ambiguous" },
    { "id": "planner_execution_divergence", "selector": "planned and actual program identity divergence remains explicit" },
    { "id": "synthetic_p5", "selector": "P5 simulation contributes zero real support" },
    { "id": "single_trace", "selector": "one trace is marked SINGLE_TRACE_HYPOTHESIS" },
    { "id": "tenant_literal", "selector": "tenant-bound private value is never a reusable literal" },
    { "id": "pii_redaction", "selector": "email phone address and customer identifiers become typed placeholders" },
    { "id": "contradictory_traces", "selector": "positive and negative evidence remain separate" },
    { "id": "malformed_incomplete", "selector": "corrupt and incomplete traces remain ineligible" }
  ]
}

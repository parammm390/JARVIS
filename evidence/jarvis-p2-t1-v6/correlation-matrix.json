{
  "task": "P2.T1",
  "projection": "finnor-os/packages/read-models/src/work-cases.ts",
  "endpoint": "/api/read-models/work-cases",
  "persistedWorkTable": false,
  "rows": [
    { "case": "instruction → one action", "sourceContract": true, "runtime": "pass" },
    { "case": "instruction → multiple actions", "sourceContract": true, "runtime": "pass" },
    { "case": "action → workflow → steps → receipt", "sourceContract": true, "runtime": "pass" },
    { "case": "pending approval no run", "sourceContract": true, "runtime": "pass" },
    { "case": "failed + recovery status priority", "sourceContract": true, "runtime": "source-contract", "reason": "P2.T1 proves terminal failure projection; recovery continuity is a later Work/cross-surface task" },
    { "case": "completed", "sourceContract": true, "runtime": "source-contract", "reason": "completed action status is covered by the unit mapping and the mixed-root integration fixture" },
    { "case": "same customer / separate instruction ≠ merged", "sourceContract": true, "runtime": "pass" },
    { "case": "same invoice / separate instruction ≠ merged without explicit root", "sourceContract": true, "runtime": "pass" },
    { "case": "no receipt ≠ outcome", "sourceContract": true, "runtime": "pass" },
    { "case": "no assignee ≠ owner", "sourceContract": true, "runtime": "source-contract", "reason": "the projection emits no inferred owner; queue rendering is P2.T2" },
    { "case": "call → agent", "sourceContract": false, "runtime": "not-proven", "reason": "no exact agent identifier exists on canonical call/voice rows" }
  ]
}

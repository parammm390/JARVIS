# P4.T7 discovery — lane and no-run cases

The authoritative instruction scope is the Thread node ID list. The presentation layer must distinguish an empty scope, action IDs waiting for a run, a synchronous trace outcome with no durable run, linked live runs, and settled linked runs. Counts must be derived per action ID rather than from tenant totals.
